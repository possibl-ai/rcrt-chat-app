/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */
export interface paths {
    '/health': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Health check */
        get: operations['getHealth'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/env-config': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Discover this deployment's auth mode before trying to sign in
         * @description Public. Tells a client what kind of deployment it is talking to, so it
         *     does not have to guess from a build-time flag that can disagree with the
         *     server. Read `auth.registration` before offering a sign-up path: it is
         *     false on most deployments, which means accounts are created by an
         *     operator (AUTH_SEED_ADMIN_*) or by invitation, not self-serve.
         */
        get: operations['getEnvConfig'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/auth/register': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Create an identity and its first password credential
         * @description Public, and **disabled by default** — check `auth.registration` on
         *     GET /v1/env-config first. A disabled deployment answers 403.
         *
         *     The 202 response is deliberately identical whether the address was free
         *     or already taken, so this endpoint cannot be used to discover who has an
         *     account here. It therefore does NOT confirm that your account was
         *     created; find out by calling POST /v1/auth/login.
         */
        post: operations['register'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/auth/login': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Exchange an email + password for a session token
         * @description **Step 1 of the bootstrap.** The platform signs its own tokens; there is
         *     no external identity provider in this path. A Firebase ID token will be
         *     rejected here and everywhere else — Firebase was removed
         *     (docs/findings/17_SELF_HOSTED_PASSWORD_AUTH.md).
         *
         *     The token is self-contained with an ~8h lifetime (AUTH_SESSION_TTL_HOURS)
         *     and **there is no refresh endpoint**: when it lapses, call this again.
         *
         *     Every credential failure returns the same 401 and the same message —
         *     wrong password, unknown address and an address with no password
         *     credential are indistinguishable by design. Do not branch on the text.
         */
        post: operations['login'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/auth/me': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The caller's identity, organisations and visible workspaces
         * @description **Step 2 of the bootstrap** — but NOT the right source for "which
         *     workspace should I use". Read `tenants` here with care:
         *
         *     - For a platform admin it lists **every tenant on the deployment**, with
         *       the role `platform_admin` where the caller holds no real membership.
         *       The literal string `platform_admin` therefore means "visible to you,
         *       not yours".
         *     - It always includes two reserved tenants created by migration
         *       20260721000001 — `Platform` (`00000000-…-000000000000`) and
         *       `Definition Registry` (`…0001`). Neither is a place to do work: the
         *       platform tenant has a trigger that rejects customer content, and the
         *       registry holds inert envelope copies of definitions.
         *
         *     Use GET /v1/auth/tenants for a list you can select from — it is
         *     membership-only. This endpoint is for the identity, the permissions and
         *     `active_tenant` (the last workspace this user selected, if any).
         */
        get: operations['getMe'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/auth/tenants': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The workspaces this identity is actually a member of
         * @description **Step 3 of the bootstrap.** Membership-only, unlike /v1/auth/me — this
         *     is the list to pick an `X-Tenant-ID` from.
         *
         *     An empty list is a normal state, not an error: it is where every new
         *     account starts. Call POST /v1/auth/signup to create your first
         *     organisation and workspace. Without a workspace, every tenant-scoped
         *     route answers `401 auth.workspace_not_selected`.
         */
        get: operations['listMyTenants'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/auth/tenants/{id}/select': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Persist a workspace as this user's default
         * @description Records the workspace as the user's last-active one, so a later
         *     /v1/auth/me returns it as `active_tenant`. It is a **convenience, not a
         *     prerequisite**: this call does not put the workspace into your session,
         *     and skipping it changes nothing. Authorization on every subsequent
         *     request comes from the `X-Tenant-ID` header, which you must send either
         *     way.
         */
        post: operations['selectTenant'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/auth/signup': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Create your first organisation and workspace
         * @description The escape from an empty workspace list, and the step that is easiest to
         *     miss: an authenticated identity with no membership can do almost nothing
         *     until it has a workspace, and POST /v1/orgs is restricted to platform
         *     admins — so this is the only self-serve route to one.
         *
         *     Only valid for an identity with **no existing org membership**; a caller
         *     who already belongs to an org gets 409. All fields are optional and
         *     default from the caller's name and email.
         *
         *     The returned `tenant_id` is what goes in `X-Tenant-ID`.
         */
        post: operations['signup'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/auth/logout': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * End this session server-side
         * @description Revokes the `user_sessions` row named by the token's `sid` claim, so the
         *     token stops authenticating on its next use. Discarding it client-side is
         *     still correct; it is no longer the only thing that happens. Idempotent.
         */
        post: operations['logout'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/breadcrumbs': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Query breadcrumbs by tags, name or parent
         * @description Returns a **bare JSON array**, not a wrapper object. There is no
         *     pagination: no cursor is accepted and none is returned. `limit` is the
         *     only control over result size, so a caller that needs more than 1000
         *     rows must narrow the filter (or use the bulk export).
         *
         *     The filters are mutually exclusive and applied in this order: `name`
         *     (exact match, returns 0 or 1), then `parent_id`, then `tags`, then most
         *     recent. Combining `name` with `tags` silently ignores the tags.
         */
        get: operations['queryBreadcrumbs'];
        put?: never;
        /**
         * Create a breadcrumb (single-row, real-time low-volume writes)
         * @description Creates a breadcrumb. For bulk writes (>=100 rows), use the bulk-loader
         *     (client.bulk.upload()) — this endpoint may reject batches >100 rows with
         *     a 400 directing the client to the bulk-loader.
         *
         *     **Not idempotent by default.** Every call creates a new row, and
         *     `(tenant_id, name)` is a non-unique index — so a retried request
         *     duplicates. Pass `upsert: true` with a stable `name` to make the write
         *     idempotent, which is what a retrying agent almost always wants.
         *
         *     The response wraps the breadcrumb: `{ breadcrumb, action }`. `action`
         *     distinguishes what happened on an upsert, and the status code follows it
         *     (201 created, 200 updated).
         */
        post: operations['createBreadcrumb'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/breadcrumbs/search': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Semantic (hybrid) search over breadcrumbs
         * @description Hybrid dense + sparse search fused with RRF, weighted equally. Returns a
         *     **bare array of breadcrumbs in rank order** — the similarity scores are
         *     computed but not serialised, so relative position is the only ranking
         *     signal a caller gets.
         *
         *     Requires a working embedding backend. Where one is not configured (local
         *     dev without embedding credentials, most commonly) this answers
         *     `500 failed to generate embedding` — which is an environment problem, not
         *     a bad query.
         */
        get: operations['semanticSearchBreadcrumbs'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/breadcrumbs/{id}': {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                id: components['parameters']['BreadcrumbId'];
            };
            cookie?: never;
        };
        /** Get a breadcrumb by id */
        get: operations['getBreadcrumb'];
        put?: never;
        post?: never;
        /** Soft-delete a breadcrumb */
        delete: operations['deleteBreadcrumb'];
        options?: never;
        head?: never;
        /**
         * Update a breadcrumb
         * @description Updates are **optimistically locked on `version`**. Read the breadcrumb
         *     first, send back the `version` you read, and the server stores
         *     `version + 1` and returns the new value. There is no way to opt out.
         *
         *     Omitting `version` is the common failure: JSON decodes the missing field
         *     to `0`, stored versions start at `1`, so the comparison never matches and
         *     the request is rejected with `409` before anything is written. Recover by
         *     re-reading the breadcrumb and retrying with the version you just read.
         *
         *     Only the fields you send are applied — omitted fields keep their stored
         *     value.
         */
        patch: operations['updateBreadcrumb'];
        trace?: never;
    };
    '/v1/chat': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Send a chat message and trigger the agent pipeline
         * @description Creates a user-message breadcrumb and publishes breadcrumb-created.
         *     The agent pipeline (context-builder -> packet-transformer -> LLM ->
         *     storyteller) runs asynchronously; the assistant response streams back
         *     via the SSE events stream (/v1/events) or the session stream.
         */
        post: operations['sendChat'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/events': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * SSE stream of breadcrumb lifecycle + edge agent events
         * @description Opens a persistent SSE connection. Events: breadcrumb.created,
         *     breadcrumb.updated, breadcrumb.deleted, agent.message, tool.progress,
         *     heartbeat. Use Last-Event-ID header for resumability.
         */
        get: operations['streamEvents'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/boards/{slug}/render': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The board slug. */
                slug: string;
            };
            cookie?: never;
        };
        /**
         * Render one board's current cells as agent-readable HTML
         * @description The Living Board's agent-perception serializer (M3). Viewer-scoped:
         *     authorization is whatever the tenant store + RLS transaction already
         *     enforce for breadcrumb reads — there is no separate permission surface
         *     for "may render the board". `tool-board.get_board` calls this over
         *     `ctx.rcrt.fetch`; the context-builder calls `internal/board.Render`
         *     directly in-process, so both callers share the exact same serializer.
         *
         *     `viewport` is a comma-separated list of focused cell ids; when present,
         *     the renderer marks those cells as in-focus and may prune distant cells
         *     for token economy.
         */
        get: operations['boardRender'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/boards/{slug}/presence': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The board slug. */
                slug: string;
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Publish one ephemeral presence tick for a board viewer
         * @description Ephemeral, never a breadcrumb — the request body is turned into an
         *     `rcrt.PresenceWire` and published onto the same tenant-wide SSE fan-out
         *     `/v1/events` already serves. `202`: fire-and-forget by design — a
         *     dropped presence tick at 3-5Hz is invisible; the next heartbeat
         *     corrects it.
         *
         *     Auth is the viewer's session (same group as `/boards/{slug}/render`).
         *     The one privileged branch is agent choreography: only a caller
         *     presenting the shared internal key may claim `actor.kind == "agent"`;
         *     a plain human session always produces a user-kind presence event.
         *     `action` is the agent choreography hint (`glide` | `act` | `idle`).
         */
        post: operations['boardPresence'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/sessions/{session_id}/stream': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** SSE stream for a specific session (chat completions) */
        get: operations['streamSessionEvents'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/sessions/{session_id}/participants': {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                session_id: components['parameters']['SessionId'];
            };
            cookie?: never;
        };
        /** List session participants (agents in a multi-agent conversation) */
        get: operations['getSessionParticipants'];
        put?: never;
        /** Add an agent to a session */
        post: operations['addSessionParticipant'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/sessions/{session_id}/participants/{agent_id}': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                session_id: components['parameters']['SessionId'];
                agent_id: string;
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Remove an agent from a session */
        delete: operations['removeSessionParticipant'];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/sessions/suspended': {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        /**
         * List this workspace's loop-detector session suspensions
         * @description Every session the loop detector has suspended in this workspace, active or already cleared, newest first. `suspended` is the live verdict rather than the stored bit: a suspension whose TTL has passed reports `false`. Requires workspace admin, for the reason the reset below does.
         */
        get: operations['listSuspendedSessions'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/sessions/{session_id}/suspension/reset': {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                session_id: components['parameters']['SessionId'];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Clear a session's loop-detector suspension
         * @description Clears the kill switch the loop detector applies when a session exceeds the event threshold, so the next turn in that session is processed again.
         *     Requires workspace admin (`CanManageTenant`) on the workspace named by `X-Tenant-ID`, because the runaway cost is borne by the workspace. The session is addressed within that workspace, so a session belonging to another workspace reads as absent rather than as forbidden.
         *     The reset does NOT disarm loop detection. The tripping condition is a windowed count of the session's own events, so a session that is still looping is suspended again on its next event; the response says so in `detector_still_armed`. A suspension also expires on its own after `ttl_seconds`, so this route is the fast path rather than the only one.
         */
        post: operations['resetSessionSuspension'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/orgs': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Create an organization */
        post: operations['createOrganization'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/orgs/{org_id}': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        /** Get an organization */
        get: operations['getOrganization'];
        /** Update an organization */
        put: operations['updateOrganization'];
        post?: never;
        /** Delete an organization */
        delete: operations['deleteOrganization'];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/orgs/{org_id}/members': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        /** List org members */
        get: operations['listOrgMembers'];
        put?: never;
        /** Add a member to an org */
        post: operations['addOrgMember'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/orgs/{org_id}/tenants': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        /** List tenants (workspaces) in an org */
        get: operations['listOrgTenants'];
        put?: never;
        /** Create a tenant (workspace) in an org */
        post: operations['createOrgTenant'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/tenants/{tenant_id}': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                tenant_id: components['parameters']['TenantId'];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Delete a tenant (workspace) */
        delete: operations['deleteTenant'];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/tenants/{tenant_id}/members': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                tenant_id: components['parameters']['TenantId'];
            };
            cookie?: never;
        };
        /** List tenant members */
        get: operations['listTenantMembers'];
        put?: never;
        /** Add a member to a tenant */
        post: operations['addTenantMember'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/tenants/{tenant_id}/api-key': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                tenant_id: components['parameters']['TenantId'];
            };
            cookie?: never;
        };
        /** Get API key status (active/revoked) */
        get: operations['getAPIKeyStatus'];
        put?: never;
        /** Generate a workspace API key (tk_*) */
        post: operations['generateAPIKey'];
        /** Revoke the active API key */
        delete: operations['revokeAPIKey'];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/tenants/{tenant_id}/usage': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                tenant_id: components['parameters']['TenantId'];
            };
            cookie?: never;
        };
        /** Get token usage for a tenant */
        get: operations['getTenantUsage'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/billing/subscription': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Get billing subscription status */
        get: operations['getBillingSubscription'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/billing/checkout': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Start a Stripe checkout session */
        post: operations['billingCheckout'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/files': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the workspace's files
         * @description Files are stored as breadcrumbs, so each entry is a breadcrumb carrying
         *     the file's metadata rather than a bespoke file object. There is no
         *     pagination and no cursor. `count` reports what was returned.
         */
        get: operations['listFiles'];
        put?: never;
        /** Upload a file (creates a file breadcrumb) */
        post: operations['uploadFile'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/files/{id}': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        /** Get file metadata */
        get: operations['getFile'];
        put?: never;
        post?: never;
        /** Delete a file */
        delete: operations['deleteFile'];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/files/{id}/content': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        /** Download file content (binary stream) */
        get: operations['getFileContent'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/bulk/breadcrumbs': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Streaming NDJSON bulk upload (bulk-loader)
         * @description Streaming bulk upload via the bulk-loader service. Content-Type is
         *     application/x-ndjson (newline-delimited breadcrumb JSON). Response is
         *     text/event-stream with per-row results (created/updated/skipped/failed).
         *     Auth: consumer-service API key (cs_*). This is the bulk SDK's primary
         *     endpoint — see docs/sdk/06_BULK_AND_CDC.md + architecture/08.
         */
        post: operations['bulkUploadBreadcrumbs'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/publish': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Publish definitions and bundle manifests to the registry
         * @description The **only** write path for published definitions. Batch: a set of
         *     definitions plus optional bundle manifests, typically one git ref's
         *     worth of changes.
         *
         *     This endpoint was previously undocumented, and its only in-repo caller
         *     was the admin console UI — which made publishing look like something
         *     that required a human in a browser. It does not: it is an ordinary
         *     authenticated JSON POST, and it is the path an agent that authors a
         *     definition at runtime should use. What was missing was this entry, not a
         *     capability.
         *
         *     **Send `dry_run: true` first.** It runs the identical validation and
         *     returns the identical per-item diff without writing anything, so an
         *     agent can check its own output before committing to it.
         *
         *     Validation is the same as bundle install: every parent chain must
         *     resolve within the batch, the platform tenant, or the already-published
         *     registry, and service dependencies must exist. A failure returns 422
         *     with a structured `validation` object naming what did not resolve — that
         *     is the response to read and act on, not to retry.
         *
         *     Publishing an item in the `engine` bundle re-syncs platform-scope engine
         *     definitions in the background, so an engine fix applies without a
         *     gateway redeploy.
         *
         *     **Auth: platform admin only.** A workspace-scoped session token gets
         *     `403 platform admin required`. `X-Tenant-ID` should be the platform
         *     tenant (`00000000-0000-0000-0000-000000000000`).
         */
        post: operations['registryPublish'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/items': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** The latest pointer of every published item */
        get: operations['registryListItems'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/items/{stem}': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The item's registry stem — a path-like identifier, e.g.
                 *     `tools/tool-project-create`. Stable across revisions; it is what
                 *     `publish` keys on.
                 */
                stem: components['parameters']['RegistryStem'];
            };
            cookie?: never;
        };
        /** The latest revision of one item, with its full definition */
        get: operations['registryGetItem'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/items/{stem}/revisions': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The item's registry stem — a path-like identifier, e.g.
                 *     `tools/tool-project-create`. Stable across revisions; it is what
                 *     `publish` keys on.
                 */
                stem: components['parameters']['RegistryStem'];
            };
            cookie?: never;
        };
        /**
         * Full revision history for one item, newest first
         * @description Definitions are included on every revision — the history *is* the audit
         *     trail, and it is what `rollback` selects a `rev` from.
         */
        get: operations['registryListRevisions'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/rollback': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Repoint an item's latest pointer at a prior revision
         * @description Does not delete anything — it moves the `latest` pointer, so the
         *     rolled-past revisions stay in the history. Like publish, a rollback in
         *     the engine bundle re-syncs engine definitions in the background.
         *     Revisions marked `deprecated` or `broken` require `force: true`.
         */
        post: operations['registryRollback'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/revisions/{stem}/{rev}/status': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Mark a registry revision deprecated or broken (envelope metadata)
         * @description Updates status fields on the revision envelope without changing the
         *     immutable definition body. Used to block unguarded rollback to known-bad
         *     revisions (Access B15).
         */
        post: operations['registrySetRevisionStatus'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/sync-engine': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Re-sync platform-scope engine definitions from the registry
         * @description Idempotent; unchanged items are skipped. Publish already triggers this in
         *     the background — call it directly only when that background sync is
         *     reported as failed.
         */
        post: operations['registrySyncEngine'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/org/{orgID}/publish': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Publish definitions and bundle manifests to an org's private scope
         * @description The org-scope write path (docs/findings/87 §2): same batch shape and
         *     validation as `/v1/registry/publish`, landing in the named
         *     organisation's private catalogue — visible to that org's admins and
         *     installable into its workspaces, never listed in the platform
         *     catalogue. Auth: org admin of this org, or platform admin; a real user
         *     session is required (no service credentials). Unlike a platform
         *     publish, an org publish never triggers the engine sync.
         */
        post: operations['registryOrgPublish'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/org/{orgID}/rollback': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Repoint an org-scope item's latest pointer at a prior revision
         * @description Same semantics as the platform rollback — a pointer move, nothing
         *     deleted, `deprecated`/`broken` revisions need `force: true` — scoped
         *     to the named org's catalogue. Never triggers an engine sync.
         */
        post: operations['registryOrgRollback'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/org/{orgID}/items': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
            };
            cookie?: never;
        };
        /** The latest pointer of every item in the org's private scope */
        get: operations['registryOrgListItems'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/org/{orgID}/items/{stem}': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
                /**
                 * @description The item's registry stem — a path-like identifier, e.g.
                 *     `tools/tool-project-create`. Stable across revisions; it is what
                 *     `publish` keys on.
                 */
                stem: components['parameters']['RegistryStem'];
            };
            cookie?: never;
        };
        /** The latest org-scope revision of one item, with its full definition */
        get: operations['registryOrgGetItem'];
        put?: never;
        post?: never;
        /**
         * Soft-delete an org-scope item's latest pointer
         * @description Revisions are kept — the item can be re-published or rolled forward
         *     again later. Removes the item from the org's catalogue listing; it does
         *     not uninstall anything from workspaces.
         */
        delete: operations['registryOrgDeleteItem'];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/org/{orgID}/items/{stem}/revisions': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
                /**
                 * @description The item's registry stem — a path-like identifier, e.g.
                 *     `tools/tool-project-create`. Stable across revisions; it is what
                 *     `publish` keys on.
                 */
                stem: components['parameters']['RegistryStem'];
            };
            cookie?: never;
        };
        /** Full org-scope revision history for one item, newest first */
        get: operations['registryOrgListRevisions'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/org/{orgID}/bundles': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
            };
            cookie?: never;
        };
        /** All bundle manifests in the org's private scope */
        get: operations['registryOrgListBundles'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/org/{orgID}/bundles/{name}': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
                /** @description Bundle name as listed by `GET /v1/registry/org/{orgID}/bundles`. */
                name: string;
            };
            cookie?: never;
        };
        /** One org-scope bundle manifest */
        get: operations['registryOrgGetBundle'];
        put?: never;
        post?: never;
        /**
         * Soft-delete an org-scope bundle manifest
         * @description Removes the manifest from the org's catalogue. Does not uninstall the
         *     bundle from any workspace that already installed it.
         */
        delete: operations['registryOrgDeleteBundle'];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/me/workspaces': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Aggregated workspace list for the authenticated user
         * @description Memberships plus org-visible discoverable workspaces, deduplicated —
         *     the end-user app switcher reads this. `joined` distinguishes a
         *     workspace the user belongs to from one their org merely lets them
         *     discover and join.
         */
        get: operations['listMyWorkspaces'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/me/model-prefs/{agent}': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The agent name whose model preference this addresses. */
                agent: string;
            };
            cookie?: never;
        };
        /**
         * Read the calling user's pinned model for one agent
         * @description The picker's "current" marker (docs/findings/89 §3-§4). A pref is a
         *     user-scope breadcrumb in the ACTIVE workspace's store, so the choice is
         *     per (workspace, user, agent). Returns `model_id` empty when no pref is
         *     set — the agent's pinned default answers.
         */
        get: operations['modelPrefGet'];
        /**
         * Pin a model for one agent for the calling user
         * @description Validates the pick against the org's available set
         *     (`availableModelsForOrg` — the same computation the picker renders), so
         *     a pref can never name a model the user was never offered. Requires
         *     breadcrumb write permission.
         */
        put: operations['modelPrefPut'];
        post?: never;
        /**
         * Clear the user's pin for one agent
         * @description The agent's own default answers again. Idempotent — clearing an already
         *     absent pref returns the empty form.
         */
        delete: operations['modelPrefDelete'];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/tenants/{tenant_id}/visibility': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Set workspace visibility (private | org | public)
         * @description `org` makes the workspace discoverable to every org member and sets a
         *     default `auto_enroll_role` of `member` when none is configured — that
         *     is what `POST /v1/tenants/{tenant_id}/join` honours. Requires tenant
         *     or org manage rights, or platform admin.
         */
        patch: operations['updateTenantVisibility'];
        trace?: never;
    };
    '/v1/tenants/{tenant_id}/directory': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                tenant_id: components['parameters']['TenantId'];
            };
            cookie?: never;
        };
        /**
         * Search the tenant directory for the Share dialog
         * @description Returns people the caller may invite to or add to this workspace, resolved
         *     from a free-text query `q` (email prefix or name). Requires
         *     `tenant:invite` rights (CanInviteToTenant). Existing members, org members
         *     and new-but-emailable addresses are returned, tagged by `kind`.
         */
        get: operations['searchTenantDirectory'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/invitations/preview': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Preview an invitation by token (pre-claim)
         * @description Look up the workspace, role and issuing workspace name for an invitation
         *     token, without consuming it. The Share dialog calls this to render the
         *     invitee's landing card before they confirm. The token is carried as a
         *     query parameter because the claimee is not yet authenticated.
         */
        get: operations['previewInvitation'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/invitations/claim': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Claim an invitation, joining the issuing workspace
         * @description Exchange an invitation token for membership of the issuing workspace.
         *     The token is single-use; a successful claim enrolls the authenticated
         *     caller as the role the inviter chose. Returns the workspace the caller
         *     has now joined.
         */
        post: operations['claimInvitation'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/images/generate': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Generate an image from a text prompt
         * @description Resolves the `image` model slot (or AZURE_OPENAI_IMAGE_DEPLOYMENT) and
         *     calls the Azure images API on behalf of the caller. Returns the image
         *     bytes as base64; the caller is expected to persist them via
         *     POST /v1/files. No signed URL is returned — bytes only.
         */
        post: operations['generateImage'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/orgs/{org_id}/bundle-selections': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The organisation whose curation this addresses. Org admin of this org, or platform admin; user session required. */
                org_id: string;
            };
            cookie?: never;
        };
        /**
         * The org's bundle selections (curation + workspace defaults)
         * @description One row per bundle the org admin has made available to the org
         *     (docs/findings/87 §3). `is_default` marks bundles installed into every
         *     workspace created afterwards; `pinned_rev` holds a bundle at a
         *     known-good revision while the registry publishes newer ones.
         */
        get: operations['orgBundleSelectionsList'];
        put?: never;
        /**
         * Create or replace a bundle selection
         * @description The bundle must resolve in the named scope's registry catalogue.
         *     Setting `is_default: true` runs the trigger-collision guard
         *     (docs/findings/87 §5) over the org's whole prospective default set —
         *     a second chat-default agent, or a non-chat-default agent triggering on
         *     conversation, is refused with `422 catalogue.trigger_collision` naming
         *     the offenders.
         */
        post: operations['orgBundleSelectionUpsert'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/orgs/{org_id}/bundle-selections/{bundle}': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: string;
                bundle: string;
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Remove a selection
         * @description Curation is not uninstall — workspaces that already installed the
         *     bundle keep it. Defaults only reach workspaces created afterwards.
         */
        delete: operations['orgBundleSelectionDelete'];
        options?: never;
        head?: never;
        /**
         * Update pinned_rev and/or is_default on a selection
         * @description Turning `is_default` on is the guarded transition (same
         *     trigger-collision guard as the upsert); turning it off is not.
         */
        patch: operations['orgBundleSelectionUpdate'];
        trace?: never;
    };
    '/v1/orgs/{org_id}/available-models': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        /**
         * Which catalog chat models this org's users may select
         * @description The picker's question (docs/findings/89 §3-§4). Any authenticated
         *     member of the org may read it — the picker needs it. Absent policy row
         *     = no lockdown: every platform-enabled chat model is available, and
         *     `lockdown` is `false` so the picker can say so. `default_model_id` is
         *     the org default when one is set.
         */
        get: operations['orgAvailableModels'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/orgs/{org_id}/model-policy': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        /**
         * The org's model allow-list and default
         * @description Returns the org's policy row, or the no-lockdown empty form
         *     (`allowed_models: []`) when no row exists. Read requires org manage
         *     rights (session-required, never the shared key).
         */
        get: operations['orgModelPolicyGet'];
        /**
         * Upsert the org's model allow-list and default
         * @description `allowed_models` is the allow-list of catalog model ids; an empty array
         *     means no lockdown. `default_model` is the optional org default. Both
         *     must reference real catalog chat models — a policy naming a model that
         *     does not exist would silently never match. Write requires org manage
         *     rights.
         */
        put: operations['orgModelPolicyPut'];
        post?: never;
        /**
         * Remove the org's policy — back to no-lockdown
         * @description Deletes the policy row; every platform-enabled chat model becomes
         *     available again. Write requires org manage rights.
         */
        delete: operations['orgModelPolicyDelete'];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/orgs/{org_id}/board-context-policy': {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        /**
         * The org's board-context lockdown
         * @description The org-level lockdown for whether any workspace in the org may feed
         *     the living board into the agent's context packet (The Living Board
         *     plan, "Admin toggle — a ladder, mirroring org_model_policies"). Absent
         *     row = no lockdown (`locked_off: false`). Read requires org manage
         *     rights.
         */
        get: operations['orgBoardContextPolicyGet'];
        /**
         * Upsert the org's board-context lockdown
         * @description `locked_off: true` disables board-context feed for every workspace in
         *     the org. Write requires org manage rights.
         */
        put: operations['orgBoardContextPolicyPut'];
        post?: never;
        /**
         * Remove the org's board-context policy — back to no-lockdown
         * @description Deletes the policy row; board-context feed is permitted again. Write
         *     requires org manage rights.
         */
        delete: operations['orgBoardContextPolicyDelete'];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/notifications': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The caller's notifications for one level, unread first
         * @description The in-app notification transport (docs/findings/87 §3). Reads are
         *     gated per level: a platform row belongs to the calling platform
         *     admin, an org row to that org's admins, a workspace row to that
         *     workspace's admins. Kinds: `bundle.update_available` (actionable —
         *     apply or dismiss), `bundle.default_install_failed` (actionable —
         *     retry), `canvas.updated` (informational deep link).
         */
        get: operations['notificationsList'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/notifications/read-all': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Mark every notification at a level read */
        post: operations['notificationsMarkAllRead'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/notifications/{id}/read': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Mark one notification read */
        post: operations['notificationMarkRead'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/notifications/{id}/apply': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Perform the action a notification offers, then mark it read
         * @description The opt-in update path (docs/findings/87 §7). For
         *     `bundle.update_available`: a workspace row updates that workspace, an
         *     org row broadcasts across the org's workspaces with the bundle
         *     installed. For `bundle.default_install_failed`: retries the default
         *     install. Informational kinds (`canvas.updated`) have no apply action
         *     — 409. Requires a user session; service credentials are refused.
         */
        post: operations['notificationApply'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/cdc/subscribe': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Register for outbound CDC (returns a per-tenant Pub/Sub subscription)
         * @description Registers the consumer service for outbound CDC. Returns a Pub/Sub
         *     subscription name the consumer reads from (or the SDK's CDC helper
         *     wraps). See architecture/08 §6.
         */
        post: operations['subscribeCDC'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/tree': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the root nodes of the definition tree
         * @description Returns the breadcrumbs at the top of the SAPL tree — those with no
         *     parent — as a **bare JSON array**, not a wrapper object.
         *
         *     Without `tag` this lists root `interpret:segment` breadcrumbs, which is
         *     the composition tree. Pass `tag` to root a different tree (for example
         *     `interpret:tool` or `interpret:renderable`).
         *
         *     The scan is capped at 200 breadcrumbs *before* the root filter is
         *     applied, so a workspace with more than 200 breadcrumbs carrying the tag
         *     can return fewer roots than it has. There is no cursor. Use
         *     `GET /v1/tree/search` or `GET /v1/breadcrumbs` when you need to page.
         */
        get: operations['treeRoot'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/tree/search': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Find definitions by name or title substring
         * @description Substring match over `name` and `title`, case-insensitive. This is
         *     **not** semantic search — it does no embedding lookup and will not find
         *     a definition by meaning. For that use `POST /v1/breadcrumbs/search`.
         *
         *     The filter is applied after the tag query, so `limit` bounds the rows
         *     scanned, not the rows returned: a search with `limit=20` can return
         *     fewer than 20 matches even when more exist.
         */
        get: operations['treeSearch'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/tree/segments': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List every composable segment, flat or as a tree
         * @description Segments are the composable units of SAPL: each carries `before`,
         *     `execute` and `after` hooks, and composing a pipeline means stacking
         *     them. This is the endpoint to call first when deciding what a
         *     composition *can* be made of.
         *
         *     The response shape depends on `tree`. Without it you get a bare array of
         *     segment breadcrumbs. With `tree=true` you get an object with both the
         *     flat `segments` array and a nested `tree` keyed by segment name, where
         *     each node reports which hooks it defines and which segments it declares
         *     a conflict with.
         */
        get: operations['treeSegments'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/tree/by-name/{name}': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Fetch one definition by name
         * @description Names are the stable handle for a definition; ids are not portable
         *     between workspaces. Resolution falls back to the platform tenant, so a
         *     workspace that has not overridden a platform definition still finds it.
         */
        get: operations['treeByName'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/tree/{id}': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Fetch one definition by id */
        get: operations['treeNode'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/tree/{id}/children': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List a definition's direct children
         * @description Direct children only — one level. Walk it yourself for a full subtree,
         *     or subscribe to `GET /v1/stream/tree/{id}` to watch one.
         */
        get: operations['treeChildren'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/tree/{id}/ancestors': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Walk a definition's parent chain
         * @description Returns the chain **child-first**, starting with the requested node and
         *     ending at the root. Note the ordering differs from the `chain` field in
         *     the resolve responses, which is root-first.
         *
         *     Only the **first** parent is followed at each level. A breadcrumb with
         *     several parents — which is how mixins are expressed — has more ancestry
         *     than this endpoint reports. Cycles terminate the walk rather than
         *     erroring.
         */
        get: operations['treeAncestors'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/tree/{id}/address': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Derive a definition's dotted address
         * @description The address is the dotted, root-first path of names that identifies a
         *     definition's position in the tree — the thing to quote in a bug report
         *     or a composition, because it survives an id change.
         */
        get: operations['treeAddress'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/resolve/{id}': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Resolve a definition's inherited content by id
         * @description Returns the definition's content **after** the parent chain has been
         *     merged into it — what actually runs, rather than what the row stores.
         *     The difference between `breadcrumb.content` and `resolved_content` is
         *     exactly what this definition inherited.
         *
         *     A definition whose parent chain cannot be resolved is a 500, not a
         *     partial merge: a composition that silently lost a layer is
         *     indistinguishable from one that never had it.
         */
        get: operations['resolveById'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/resolve/by-name/{name}': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Resolve a definition's inherited content by name
         * @description As `GET /v1/resolve/{id}`, keyed by the portable name. This is the call
         *     an agent should make before invoking a tool it did not author: it shows
         *     the effective configuration including everything inherited.
         */
        get: operations['resolveByName'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/resolve/diff': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Show what a child overrides from its parent
         * @description Answers "what does this definition actually change?". Both sides are
         *     resolved first, then the child's **own** content is compared against the
         *     parent's resolved content, so `overrides` names only the keys the child
         *     introduces or changes.
         *
         *     The comparison is a top-level key comparison on formatted values, not a
         *     structural deep diff: a change nested inside an object shows as the
         *     whole object having changed, and two values that format identically
         *     (`1` and `1.0`) are treated as equal.
         */
        post: operations['resolveDiff'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/compose/validate': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Resolve a segment stack and check it for conflicts
         * @description Takes an ordered list of segment names and reports whether they compose:
         *     every name must resolve to a breadcrumb tagged `interpret:segment`, and
         *     no two may declare each other in their `meta.conflicts`.
         *
         *     **A failed validation is still a 200.** The verdict is the `valid`
         *     field, not the status code — check `valid`, not `response.ok`. A 4xx
         *     here means the request was malformed, not that the composition was
         *     rejected.
         */
        post: operations['composeValidate'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/compose/preview': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Preview a segment stack (alias of compose/validate)
         * @description Identical to `POST /v1/compose/validate` — same handler, same request,
         *     same response. It exists because "preview" is what the authoring UI
         *     calls the action. Prefer `validate` in new code.
         */
        post: operations['composePreview'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/compose/suggest': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Suggest segments for a stated intent
         * @description Ranks the workspace's segments against a natural-language intent and
         *     returns the top five. The scoring is **keyword overlap**, not embedding
         *     similarity: each word of the intent scores 2 for appearing in a segment
         *     name and 1 for appearing in its description. An intent that shares no
         *     words with any segment returns an empty list, however close it is in
         *     meaning.
         */
        get: operations['composeSuggest'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/compose/execute': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Retired — always 410
         * @deprecated
         * @description **Gone, and it never worked.** The handler this replaced resolved the
         *     segments, built a tool request, logged it, and answered
         *     `{"status": "submitted"}` having forwarded it nowhere: no HTTP call, no
         *     publish, no breadcrumb write. It is documented here rather than deleted
         *     because a published SDK carried a typed method for it, and a caller who
         *     reads *why* it is gone learns something a bare 404 would not tell them.
         *
         *     It is retired rather than repaired: runtime composition with no human in
         *     the loop contradicts the propose-approve-live model
         *     (`docs/findings/30_SAPL_VISION_AND_REPAIR.md` §6.2). Use
         *     `POST /v1/compose/validate` to resolve and check a composition, then
         *     invoke a seeded tool through the normal dispatch path.
         */
        post: operations['composeExecute'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/render/{name}/resolve': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Resolve a renderable and its wrapper layers
         * @description Returns everything needed to render a UI fragment: the resolved content
         *     plus the ordered `layers` — the parent chain's before/after hooks that
         *     wrap it, with the renderable's own hooks last and deepest.
         *
         *     Unlike tool resolution this **degrades rather than fails**. A renderable
         *     always contributes its own hooks, so a broken parent chain yields an
         *     unwrapped render with a warning in the service logs, not a 500. If a
         *     card renders without its frame, this is why.
         *
         *     `meta.service_config` is stripped from every layer — renderables never
         *     receive credentials. Responses are cacheable for 60 seconds.
         */
        get: operations['resolveRenderable'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/stream/tree/{id}': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Subscribe to changes under a subtree (SSE)
         * @description `text/event-stream`. Emits a `connected` event carrying the subtree's
         *     root name and direct-child count, then a breadcrumb event whenever
         *     anything in the subtree is created, updated or deleted. A comment
         *     heartbeat is sent every 30 seconds so an idle connection is not reaped
         *     by an intermediary.
         *
         *     The stream is exempt from the gateway's request timeout. It carries
         *     events for the caller's own workspace only.
         */
        get: operations['streamTree'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/auth/complete': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * OAuth landing page (HTML, not an API call)
         * @description Serves a static HTML page that posts `{type: 'RCRT_AUTH_COMPLETE'}` to
         *     `window.opener` and closes itself. It is the redirect target for
         *     popup-based sign-in in the browser extension and desktop apps.
         *
         *     It is **not** an API endpoint: it takes no parameters, requires no
         *     credential, returns no JSON, and completes no server-side exchange. An
         *     SDK should not generate a client method for it. It is documented so that
         *     a reader who finds it in the route table knows it is a page.
         */
        get: operations['authComplete'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/auth/password': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Change the caller's own password
         * @description Requires the current password — a session token alone is not enough,
         *     because a stolen token is exactly the case this defends against.
         *
         *     **Every other session is revoked on success.** A password change is how
         *     a person responds to believing their credential is compromised, so the
         *     sessions opened with the old password end with it. The caller's current
         *     session survives. `sessions_revoked` reports how many ended.
         *
         *     Rate-limited alongside the other credential endpoints. Rejected outright
         *     for workspace-API-key callers: a `tk_` key has no password to change.
         */
        post: operations['changePassword'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/auth/session': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Check whether the caller's session is still usable
         * @description Answers `{"status": "active"}` when the session is live. It exists for
         *     browser clients: they already watch their own token's `exp`, but a
         *     revoked or disabled session has a perfectly good `exp`, so that watch
         *     cannot see it.
         *
         *     Reaching a 200 means the middleware already found the session live,
         *     which is why the body is flat. **The interesting answer is the 401**,
         *     which carries the `code` a client should act on — and note that not
         *     every 401 means "sign out": `workspace not selected` is a 401 too, and
         *     signing a user out over it makes a fresh deployment unenterable.
         */
        get: operations['getSessionState'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/auth/sessions': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the caller's own active sessions
         * @description One entry per device the caller is signed in on, with the current one
         *     flagged. This is the data behind a "where you're signed in" screen.
         *
         *     Scoped to the caller by the SQL predicate, not by a check in the
         *     handler: there is no parameter that could widen it to another user.
         *     Acting on somebody else's sessions is a platform-admin operation under
         *     `/v1/platform/users/{user_id}/…`.
         *
         *     Rejected for workspace-API-key callers — a `tk_` key has no sessions.
         */
        get: operations['listSessions'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/auth/sessions/{id}': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * End one of the caller's own sessions
         * @description "Sign me out of that other device." Revoking the *current* session is
         *     permitted and is equivalent to signing out — the next request with that
         *     token is refused like any other revoked session.
         *
         *     A session that is not the caller's own and a session that is already
         *     gone both answer 404, deliberately: distinguishing them would make this
         *     endpoint an oracle for other people's session ids.
         */
        delete: operations['revokeSession'];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/auth/sessions/revoke-others': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * End every session except the caller's current one
         * @description "Sign out everywhere else." The caller's current session survives, so
         *     this does not lock the caller out of the app they invoked it from.
         *     `sessions_revoked` reports how many ended.
         */
        post: operations['revokeOtherSessions'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/auth/delete-account': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Delete the caller's own account and its orphaned data
         * @description Irreversible. Runs as one transaction: memberships are removed, then
         *     every workspace and org the caller was a member of that now has **zero
         *     members** is deleted outright, taking its breadcrumbs, sessions and
         *     service grants with it by cascade. The response reports the counts so a
         *     client can confirm the cascade actually ran.
         *
         *     Deleting the last member of a shared workspace therefore deletes the
         *     workspace. Check the membership of anything you care about first.
         *
         *     One residual window to know about: session tokens are signed rather than
         *     stored, so a token issued before the delete stays cryptographically
         *     valid until it expires and its bearer can re-materialise a user row. The
         *     credential is gone with the account, so nobody can sign in again; the
         *     window is bounded by the token TTL.
         *
         *     Rejected for workspace-API-key callers.
         */
        post: operations['deleteAccount'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/breadcrumbs/aggregate': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Count, sum or average over breadcrumbs server-side
         * @description Computes an aggregate in the database instead of shipping rows to the
         *     client. Tag matching is AND across every tag given, with the same
         *     tenant-plus-platform scoping as `GET /v1/breadcrumbs`.
         *
         *     `group_by` buckets by the value *after* a tag prefix: `group_by:
         *     "deal-stage"` groups rows tagged `deal-stage:won` and
         *     `deal-stage:lost` into `won` and `lost`. Without it there is one
         *     ungrouped `total`.
         *
         *     `field` is required for `sum` and `avg` and is a dot path within the
         *     breadcrumb content (`amount` and `content.amount` mean the same thing).
         *     It is restricted to letters, digits, underscores and dots, and is bound
         *     as a parameter rather than interpolated into SQL.
         */
        post: operations['aggregateBreadcrumbs'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/bundles': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List published bundles
         * @description Bundles group definitions into something installable. This lists what
         *     the registry has; `GET /v1/registry/bundles/{name}` returns one
         *     bundle's manifest.
         */
        get: operations['listRegistryBundles'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/registry/bundles/{name}': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Fetch one bundle's manifest
         * @description The manifest names the definitions the bundle installs and the version
         *     of each. Read it before installing to see what a workspace is about to
         *     receive.
         */
        get: operations['getRegistryBundle'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/files/{id}/text': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Fetch a file's extracted text
         * @description Returns the text a parser extracted from the upload — this is what to
         *     feed a model, rather than the raw bytes from
         *     `GET /v1/files/{id}/content`.
         *
         *     The response format is chosen by the `Accept` header, and the match is
         *     **exact**: `Accept: text/plain` yields plain text, and anything else —
         *     including `text/plain; charset=utf-8` — yields the JSON object.
         *     `word_count` is an estimate derived from the character count, not a
         *     count of words.
         */
        get: operations['getFileText'];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/files/{id}/extraction': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Replace a file's extracted text and metadata
         * @description Writes an extraction result back onto the file — the path a parser or an
         *     agent uses to correct or supply text the platform could not extract
         *     itself.
         */
        patch: operations['updateFileExtraction'];
        trace?: never;
    };
    '/v1/knowledge/batch': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Submit up to 100 knowledge items in one call
         * @description Per-item results, not all-or-nothing: the response is **207
         *     Multi-Status** and each item reports its own `status` of `created`,
         *     `updated` or `failed`. A caller that checks only the HTTP status will
         *     miss failures.
         *
         *     Batches over 100 items are refused with a 400 whose error carries a
         *     `redirect` field naming the bulk-loader (`POST /v1/bulk/breadcrumbs`,
         *     streaming NDJSON), which is the right tool above that size.
         *
         *     `type` applies to every item; per-item `visibility` still governs who
         *     can write it, so a batch mixing scopes can partially fail on
         *     permissions.
         */
        post: operations['batchKnowledge'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/knowledge/{type}': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List knowledge items of one type
         * @description Items scoped to another individual user are filtered out unless the
         *     caller is that user or a workspace admin, so two members of the same
         *     workspace can see different totals from the same call. `total` counts
         *     what was returned, not what exists.
         */
        get: operations['listKnowledge'];
        put?: never;
        /**
         * Create a typed knowledge item
         * @description The typed front door to breadcrumb storage: you supply a `type`, an
         *     `external_id` from your own system and a `data` object, and the platform
         *     applies the tag set that makes the item retrievable by agents. Writing
         *     the equivalent breadcrumb by hand is easy to get wrong — an item stored
         *     without the corpus tags is embedded and searchable yet invisible to
         *     every agent, with no error on either side.
         *
         *     `external_id` is the upsert key. Set `upsert: true` and a retry updates
         *     rather than duplicating, which is what a retrying agent almost always
         *     wants.
         *
         *     **`visibility` decides who may write, not just who may read**: `org`
         *     needs org-admin, `workspace` (the default) needs workspace-admin, and
         *     `user:<id>` or `agent:<name>` need only member. A member writing at the
         *     default scope gets a 403.
         */
        post: operations['createKnowledge'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/knowledge/{type}/{external_id}': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Fetch one knowledge item by its external id */
        get: operations['getKnowledge'];
        /**
         * Replace a knowledge item wholesale
         * @description Full replacement, and it always upserts — a PUT to an external id that
         *     does not exist creates it.
         *
         *     When the item does exist, the caller needs permission for **both** the
         *     existing scope and the new one. That stops a workspace admin quietly
         *     pulling an org-scoped item down to workspace scope.
         */
        put: operations['replaceKnowledge'];
        post?: never;
        /**
         * Delete a knowledge item
         * @description Permission is checked against the item's existing visibility, so an
         *     org-scoped item needs org-admin to remove.
         */
        delete: operations['deleteKnowledge'];
        options?: never;
        head?: never;
        /**
         * Merge fields into a knowledge item
         * @description Merges `data` into the stored object at the top level and appends
         *     `tags`. Visibility is not settable here — it is inferred from the item's
         *     existing tags and used for the permission check. Use PUT to change
         *     scope.
         */
        patch: operations['patchKnowledge'];
        trace?: never;
    };
    '/v1/customer/chat': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Chat completion on the customer namespace
         * @description The same handler as `POST /v1/chat`, reached through the customer
         *     namespace. Identical request, response and auth.
         *
         *     The namespace exists so an external integration can depend on a surface
         *     that will not move as internal routes are reorganised. Prefer it over
         *     `/v1/chat` when you are writing an integration you will not be
         *     redeploying often.
         */
        post: operations['customerChat'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/customer/knowledge/batch': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Submit up to 100 knowledge items (customer namespace)
         * @description The same handler as `POST /v1/knowledge/batch`, on the stable customer
         *     namespace. See that operation for behaviour.
         */
        post: operations['customerBatchKnowledge'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/customer/knowledge/{type}': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** List knowledge items of one type (customer namespace) */
        get: operations['customerListKnowledge'];
        put?: never;
        /**
         * Create a typed knowledge item (customer namespace)
         * @description The same handler as `POST /v1/knowledge/{type}`, on the stable customer
         *     namespace. See that operation for the visibility rules.
         */
        post: operations['customerCreateKnowledge'];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    '/v1/customer/knowledge/{type}/{external_id}': {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Fetch one knowledge item (customer namespace) */
        get: operations['customerGetKnowledge'];
        /** Replace a knowledge item wholesale (customer namespace) */
        put: operations['customerReplaceKnowledge'];
        post?: never;
        /** Delete a knowledge item (customer namespace) */
        delete: operations['customerDeleteKnowledge'];
        options?: never;
        head?: never;
        /** Merge fields into a knowledge item (customer namespace) */
        patch: operations['customerPatchKnowledge'];
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /**
         * @description The calling user's pinned model for one agent (per workspace). An empty
         *     `model_id` means no pin — the agent's own default answers.
         */
        ModelPref: {
            /** @description The agent name this pref applies to. */
            agent: string;
            /** @description The pinned catalog model id; absent when no pref is set. */
            model_id?: string;
        };
        /**
         * @description The org-level model allow-list and default. Absent row = no lockdown:
         *     every platform-enabled chat model is available.
         */
        OrgModelPolicy: {
            /** Format: uuid */
            org_id: string;
            /** @description Catalog model ids the org permits; empty = no lockdown. */
            allowed_models: string[];
            /** @description The org-default catalog model id, if set. */
            default_model?: string | null;
            /**
             * Format: uuid
             * @description The actor who last wrote the policy.
             */
            updated_by?: string;
            /**
             * Format: date-time
             * @description When the policy was last written.
             */
            updated_at?: string;
        };
        /**
         * @description The picker's whole answer — which catalog chat models the org's users
         *     may select, plus the org default. `lockdown: false` means every
         *     platform-enabled chat model is available.
         */
        AvailableModels: {
            models: {
                /** @description The catalog model id. */
                id: string;
                /** @description The provider address (e.g. `vertex_ai/...`). */
                address: string;
                /** @description Human-readable label for the picker. */
                label?: string;
            }[];
            /** @description The org-default catalog model id, if set. */
            default_model_id?: string;
            /** @description Whether the org has an allow-list at all. */
            lockdown: boolean;
        };
        /**
         * @description The org-level board-context lockdown. Absent row = no lockdown
         *     (`locked_off: false`).
         */
        OrgBoardContextPolicy: {
            /** Format: uuid */
            org_id: string;
            /** @description When true, no workspace in the org feeds the board into context. */
            locked_off: boolean;
            /**
             * Format: uuid
             * @description The actor who last wrote the policy.
             */
            updated_by?: string;
            /**
             * Format: date-time
             * @description When the policy was last written.
             */
            updated_at?: string;
        };
        /**
         * @description The Living Board's agent-perception serializer output — the semantic-
         *     spatial HTML the agent reads, plus telemetry about the render.
         */
        BoardRender: {
            /** @description The board slug. */
            slug: string;
            /** @description The rendered semantic-spatial HTML. */
            html: string;
            /** @description Number of board cells rendered. */
            cell_count: number;
            /** @description Number of regions the renderer produced. */
            region_count: number;
            /** @description Whether the render is full-fidelity (1) or pruned (0). */
            full_fidelity: number;
            /** @description Estimated token cost of the rendered HTML. */
            token_estimate: number;
        };
        /**
         * @description The wire shape a viewer's browser (human) or tool-board (agent, via the
         *     internal key) posts. At least one of `cursor`, `selection`, or `action`
         *     is required. Fields beyond `cursor`, `selection`, `name`, and `action`
         *     are honoured ONLY when the caller is internal — a non-internal caller
         *     supplying them gets a user-kind presence event regardless.
         */
        BoardPresenceRequest: {
            /** @description A position in BOARD coordinates (not screen pixels). */
            cursor?: {
                x: number;
                y: number;
            };
            /** @description Selected cell ids. */
            selection?: string[];
            /** @description Display name the caller supplies (trusted for humans). */
            name?: string;
            /** @description Agent identifier (internal callers only). */
            agent_id?: string;
            /** @description Agent display name (internal callers only). */
            agent_name?: string;
            /**
             * @description Agent choreography hint (internal callers only).
             * @enum {string}
             */
            action?: 'glide' | 'act' | 'idle';
        };
        HealthStatus: {
            /** @enum {string} */
            status: 'ok' | 'degraded' | 'down';
            version?: string;
            commit_sha?: string;
        };
        /** @description Public deployment configuration, from GET /v1/env-config. */
        EnvConfig: {
            auth: {
                /**
                 * @description Always `password` on this platform.
                 * @enum {string}
                 */
                mode: 'password';
                /** @description Path to the sign-in endpoint. */
                login: string;
                register: string;
                /** @enum {string} */
                token_type: 'Bearer';
                /**
                 * @description Whether POST /v1/auth/register is accepted. False on most
                 *     deployments — accounts then come from an operator seed or an
                 *     invitation.
                 */
                registration: boolean;
                /**
                 * @description True only where the gateway runs with AUTH_BYPASS (local dev),
                 *     in which case the internal API key resolves to the seeded
                 *     platform admin. Never true on a real deployment.
                 */
                bypass?: boolean;
            };
            capabilities?: {
                [key: string]: unknown;
            };
            env_name?: string;
            gcp_project_id?: string;
        };
        RegisterRequest: {
            /** Format: email */
            email: string;
            /** @description Must satisfy the deployment's password policy. */
            password: string;
            /** @description Optional; defaults to the local part of the address. */
            name?: string;
        };
        /**
         * @description Fixed body, identical whether an account was created or the address was
         *     already taken. It is not a confirmation — call POST /v1/auth/login to
         *     find out.
         */
        RegisterAccepted: {
            /** @enum {string} */
            status: 'ok';
            message: string;
        };
        LoginRequest: {
            /** Format: email */
            email: string;
            password: string;
        };
        /**
         * @description OAuth-2.0-shaped so an SDK can treat it like any other token response —
         *     but note there is no `refresh_token`, because there is no refresh
         *     endpoint.
         */
        LoginResponse: {
            /** @description The session JWT. Send as `Authorization: Bearer <token>`. */
            access_token: string;
            /** @enum {string} */
            token_type: 'Bearer';
            /** @description Seconds until expiry. */
            expires_in: number;
            /** Format: date-time */
            expires_at: string;
            /**
             * @description Enough identity to render a name without an immediate /v1/auth/me
             *     round trip. It carries no workspace — that still needs
             *     GET /v1/auth/tenants.
             */
            user: {
                /** Format: uuid */
                id: string;
                email: string;
                name: string;
            };
        };
        MeResponse: {
            /** @description The authenticated identity as resolved from the token. */
            user: {
                [key: string]: unknown;
            };
            /**
             * @description When true, `tenants` below is every tenant on the deployment, not a
             *     membership list. Read it accordingly.
             */
            is_platform_admin: boolean;
            organizations: components['schemas']['OrgWithRole'][];
            /** Format: uuid */
            active_org?: string;
            /**
             * @description Workspaces **visible** to the caller — see this endpoint's
             *     description for why that is not the same as selectable. Use
             *     GET /v1/auth/tenants to choose one.
             */
            tenants: components['schemas']['TenantWithRole'][];
            /**
             * Format: uuid
             * @description The workspace this user last selected, if any. Absent for an account
             *     that has never selected one, and absent when the user has several and
             *     has chosen none — so it cannot be relied on as "the" workspace.
             */
            active_tenant?: string;
            /** @description The caller's permissions on `active_tenant`; empty when there is none. */
            permissions: string[];
            grants?: {
                [key: string]: unknown;
            }[];
        };
        OrgWithRole: {
            /** Format: uuid */
            id: string;
            name: string;
            slug?: string;
            role: string;
        };
        TenantWithRole: {
            /** Format: uuid */
            id: string;
            name: string;
            slug?: string;
            /** Format: uuid */
            org_id?: string;
            org_name?: string;
            /**
             * @description The caller's role. On /v1/auth/me the literal value
             *     `platform_admin` means "visible to you, but you are not a member" —
             *     it is a COALESCE default, not a membership.
             */
            role: string;
        };
        /**
         * @description From GET /v1/auth/tenants. Membership-only, so every entry here is a
         *     workspace the caller may put in `X-Tenant-ID`. An empty array is a normal
         *     first-run state.
         */
        TenantListResponse: {
            tenants: components['schemas']['TenantWithRole'][];
        };
        TenantSelectResponse: {
            /** Format: uuid */
            tenant_id: string;
            message: string;
        };
        /** @description All fields optional; sensible defaults are derived server-side. */
        SignupRequest: {
            /** @description Defaults from the caller's display name or email local-part. */
            org_name?: string;
            /** @default Personal */
            workspace_name: string;
            /** @description Email domains auto-joined to the new org. Empty by default. */
            allowed_domains?: string[];
        };
        /**
         * @description The new org and its first workspace. `tenant_id` is what goes in
         *     `X-Tenant-ID` from here on.
         */
        SignupResponse: {
            /** Format: uuid */
            org_id: string;
            /** Format: uuid */
            tenant_id: string;
        };
        LogoutResponse: {
            /** @enum {string} */
            status: 'ok';
            /**
             * @description Whether a server-side session row was actually revoked. False for a
             *     credential that has no session (e.g. an API key).
             */
            session_revoked: boolean;
            message: string;
        };
        /**
         * @description Structured error envelope (per architecture/06_LOGGING_SYSTEM.md §4).
         *     The `code` field is a stable, dotted identifier consumers match on.
         *
         *     **Universal.** Every response with a status >= 400 from api-gateway and
         *     from the bulk routes has this body. `code`, `message` and `retryable` are
         *     always present, and `remediation` is present on every failure the gateway
         *     can name a next step for — which is all of them. It holds for failures no
         *     handler wrote, too: an unknown path, an unsupported method, a panic and a
         *     deadline all arrive in this shape. `Content-Type` is always
         *     `application/json`.
         *
         *     This is enforced at one seam (`services/internal/httperr.Normalize`)
         *     rather than at each call site, which is why it can be stated as an
         *     invariant: a handler that emits a flat `{"error":"string"}`, a plain-text
         *     body or no body at all is converted on the way out. A handler that
         *     already emits a conforming envelope is passed through untouched, so a
         *     precise code such as `auth.workspace_not_selected` always wins over the
         *     status-derived one.
         *
         *     `error.code` has two tiers, both registered in docs/error-codes.md:
         *
         *     - **Transport** — derived from the HTTP status; sixteen codes covering
         *       every failure (`client.bad_request`, `auth.unauthorized`,
         *       `auth.forbidden`, `client.not_found`, `client.method_not_allowed`,
         *       `client.conflict`, `client.rate_limited`, `server.internal`,
         *       `server.unavailable`, `server.timeout`, …). Predictable on every route,
         *       so a consumer can branch on it without knowing which handler answered.
         *     - **Precise** — a domain code a route sets deliberately
         *       (`auth.workspace_not_selected`, `platform.scope_violation`, `bulk.*`).
         *       Strictly more specific than the transport code for the same status.
         *
         *     Match on `code` first and treat an unrecognised one as its transport tier
         *     by status. **Do not infer the cause from the status alone**: 401 on this
         *     gateway means either a bad credential or a valid credential with no
         *     workspace selected, and only `code` separates them.
         *
         *     5xx messages are deliberately generic — the underlying detail is in the
         *     gateway logs, correlated by the `X-Request-Id` on the response, and is
         *     withheld from the body on purpose.
         */
        Error: {
            error: {
                /**
                 * @description Stable dotted error code (e.g. client.not_found,
                 *     auth.workspace_not_selected, platform.scope_violation,
                 *     bulk.validation.failed). Always present. Every code is registered
                 *     in docs/error-codes.md.
                 */
                code: string;
                /**
                 * @description What went wrong. Prose, for a human or a log. Always present.
                 *     Never a raw internal error: a handler message carrying driver
                 *     text, a dial failure, a stack frame or a container path is
                 *     replaced with a safe one for its status and logged instead.
                 */
                message: string;
                /**
                 * @description Whether re-sending the identical request could succeed. Always
                 *     present. False means a retry is wasted work — change something
                 *     first.
                 * @default false
                 */
                retryable: boolean;
                /**
                 * @description What to do about it: the concrete next call, header or field.
                 *     Present on every failure the gateway can name a step for, which
                 *     in practice is all of them. This is the difference between "you
                 *     may not do this" and "here is what you may do instead".
                 */
                remediation?: string;
                /**
                 * @description On `auth.workspace_not_selected`: the workspace ids that would
                 *     actually work in `X-Tenant-ID`, already filtered to real
                 *     memberships and with the two reserved tenants removed. An empty
                 *     array means the caller belongs to no workspace yet and needs
                 *     POST /v1/auth/signup.
                 */
                selectable_workspaces?: components['schemas']['WorkspaceOption'][];
                /**
                 * @description On 405 (`client.method_not_allowed`): the HTTP methods this path
                 *     does serve. Re-send with one of these.
                 * @example [
                 *       "GET",
                 *       "POST"
                 *     ]
                 */
                allowed_methods?: string[];
                /**
                 * @description On 429/503 when the gateway sent a `Retry-After` header: the same
                 *     value, in the body, so a client reading only the body still knows
                 *     how long to wait.
                 */
                retry_after_seconds?: number;
                /**
                 * Format: int64
                 * @description Set when the request body exceeded the route's cap: the size that
                 *     would have been accepted. A truncated body fails to parse, so
                 *     without this the response reads as "invalid request body" for a
                 *     payload that was merely too big.
                 */
                max_bytes?: number;
                /**
                 * @description Anything the originating handler sent alongside its message that
                 *     the fields above do not model — most usefully a field-level
                 *     `errors` list from a validation handler. Present only when the
                 *     handler produced one; the keys are handler-specific, so treat it
                 *     as a diagnostic aid rather than a contract.
                 */
                details?: {
                    [key: string]: unknown;
                };
                /** @description In-repo doc path covering this condition. */
                docs?: string;
                /** @description Nested error envelope for wrapped errors. */
                cause?: {
                    code?: string;
                    message?: string;
                };
            };
        };
        /** @description One workspace the caller may put in `X-Tenant-ID`. */
        WorkspaceOption: {
            /** Format: uuid */
            id: string;
            name: string;
            /**
             * @description The caller's role on this workspace (e.g. `tenant_owner`, `member`).
             *     Never `platform_admin` here — this list is memberships only.
             */
            role: string;
        };
        /**
         * @description Universal data container. Everything in RCRT is a breadcrumb (agents,
         *     tools, messages, memories, configs, knowledge, etc.).
         */
        Breadcrumb: {
            /** Format: uuid */
            id: string;
            /**
             * Format: uuid
             * @description The workspace this row belongs to. Always serialised.
             */
            tenant_id: string;
            name: string;
            title?: string;
            /** @description Typed content payload (JSONB) — shape depends on tags. */
            content?: {
                [key: string]: unknown;
            };
            tags: string[];
            /**
             * @description Ordered UUID list encoding hierarchy. Named `parent_ids`; this field
             *     was previously documented as `parent_chain`, which the gateway has
             *     never emitted.
             */
            parent_ids?: string[];
            created_by?: components['schemas']['Actor'];
            /**
             * @description Optimistic-concurrency counter, incremented on each update. Send it
             *     back on PATCH to detect a lost update.
             */
            version: number;
            /** Format: date-time */
            created_at: string;
            /**
             * Format: date-time
             * @description Zero-valued (`0001-01-01T00:00:00Z`) on the object returned by a
             *     create — the create response echoes the in-memory row before the
             *     stored timestamp is read back. Re-read the breadcrumb if you need it.
             */
            updated_at?: string;
            /**
             * @description Opaque, caller-supplied hash stored verbatim for CDC change
             *     detection. RCRT never computes it. Absent means "no hash recorded",
             *     which clients must treat as "always changed".
             */
            content_hash?: string;
            /**
             * @description Marketplace/bundle lifecycle state, when applicable.
             * @enum {string}
             */
            status?: 'installed' | 'uninstall';
            /**
             * Format: date-time
             * @description Absolute expiry. The scheduler's hygiene job soft-deletes rows whose
             *     `ttl` has passed. Used for ephemeral telemetry writes.
             */
            ttl?: string | null;
            /** Format: date-time */
            deleted_at?: string | null;
        };
        BreadcrumbCreate: {
            name: string;
            title?: string;
            content: {
                [key: string]: unknown;
            };
            tags: string[];
            /**
             * @description Parent breadcrumb ids (for hierarchy). An array named `parent_ids`;
             *     this field was previously documented as a single `parent_id`, which
             *     the gateway does not read.
             */
            parent_ids?: string[];
            /**
             * @description Match an existing row on `(tenant_id, name)` and update it instead of
             *     inserting. Defaults to false when omitted. **Set it on any write you
             *     might retry** — otherwise each attempt creates a duplicate row, and
             *     `name` is not unique.
             */
            upsert?: boolean;
            /**
             * Format: date-time
             * @description Absolute expiry for an ephemeral breadcrumb.
             */
            ttl?: string;
            /** @description Opaque caller-supplied hash, stored verbatim for CDC. */
            content_hash?: string;
            created_by?: components['schemas']['Actor'];
        };
        /**
         * @description Every field except `version` is optional, and an omitted field leaves the
         *     stored value alone. `version` is required: the server compares it to the
         *     stored version and returns `409` on any mismatch, including the `0` that
         *     an omitted field decodes to.
         */
        BreadcrumbUpdate: {
            /**
             * @description The version you read from the breadcrumb. Must equal the stored
             *     version or the update is rejected with `409`. On success the server
             *     stores `version + 1` and returns it on the response body.
             */
            version: number;
            name?: string;
            title?: string;
            /** @description Replaces the parent list outright when present. */
            parent_ids?: string[];
            content?: {
                [key: string]: unknown;
            };
            /**
             * @description Replaces the tag list outright when present. Setting a `for-user:`
             *     tag naming somebody other than the caller requires an org-admin or
             *     platform-admin role, and is otherwise rejected with `403`.
             */
            tags?: string[];
            /**
             * @description Opaque, caller-defined hash stored verbatim for CDC sync. RCRT never
             *     computes it. Omit the field to preserve the stored value; send it
             *     (including an empty string) to overwrite.
             */
            content_hash?: string;
        };
        /**
         * @description A bare array of breadcrumbs — what GET /v1/breadcrumbs and
         *     GET /v1/breadcrumbs/search both return.
         *
         *     This replaces the `BreadcrumbQueryResult` and `BreadcrumbSearchResult`
         *     wrapper objects that used to be documented here. Neither has ever been
         *     emitted: there is no `items` envelope, no `next_cursor`, and search does
         *     not attach per-result `score` values.
         */
        BreadcrumbList: components['schemas']['Breadcrumb'][];
        /**
         * @description What POST /v1/breadcrumbs returns. The breadcrumb is wrapped, and
         *     `action` reports which of the two things an upsert did.
         */
        BreadcrumbWriteResult: {
            breadcrumb: components['schemas']['Breadcrumb'];
            /**
             * @description `created` (HTTP 201) or `updated` (HTTP 200). Redundant with the
             *     status code, and cheaper to branch on.
             * @enum {string}
             */
            action: 'created' | 'updated';
        };
        /** @description Provenance — who/what created the breadcrumb. */
        Actor: {
            /** @enum {string} */
            type?: 'user' | 'agent' | 'tool' | 'system' | 'scheduler';
            id?: string;
        };
        ChatRequest: {
            /** @description The user's chat message. */
            message: string;
            /** @description Existing session id (omit to start a new session). */
            session_id?: string;
            /** @description Explicit agent name to route to (omit for default). */
            target_agent?: string;
            /** @description End-user id (for API-key callers without a JWT). */
            user_id?: string;
            extra_tags?: string[];
            attachments?: components['schemas']['ChatAttachment'][];
        };
        ChatAttachment: {
            file_id?: string;
            mime_type?: string;
            uri?: string;
        };
        /**
         * @description The accepted user message. `id` is the created user-message breadcrumb —
         *     the assistant's reply is not here, it arrives on the SSE stream.
         */
        ChatResponse: {
            /**
             * Format: uuid
             * @description The user-message breadcrumb just created. Named `id`, not
             *     `message_id`.
             */
            id: string;
            session_id: string;
            /**
             * @description Present only when the org is at or near its plan cap; absent when
             *     usage is fine. Soft enforcement — the request is never blocked.
             * @enum {string}
             */
            usage_status?: 'warn' | 'over';
        };
        /**
         * @description SSE event stream. Each event has an optional `event` (type), `data`
         *     (JSON payload), `id` (for Last-Event-ID resumability), `retry`
         *     (reconnection interval ms). The SDK parses this into an async iterator.
         */
        SSEEventStream: {
            /** @description Event type (e.g. breadcrumb.created, heartbeat, agent.message). */
            event?: string;
            /** @description JSON-encoded event payload. */
            data?: string;
            id?: string;
            retry?: number;
        };
        /** @description Per-row result event from the bulk-loader SSE stream. */
        BulkResultStream: {
            /** @constant */
            event?: 'result';
            data?: {
                row?: number;
                /** @enum {string} */
                status?: 'created' | 'updated' | 'skipped' | 'failed';
                error?: string;
            };
        };
        /**
         * @description One publishable definition — a tool, agent, config or any other
         *     marketplace item. The `tags` decide what it is; `content` is the typed
         *     payload for that kind.
         *
         *     There is no JSON Schema for the per-kind `content` shapes yet, so an
         *     agent authoring one must follow an existing example. `dry_run` on
         *     publish is the substitute: it runs full validation and reports what
         *     would not resolve, without writing.
         */
        RegistryDefinition: {
            /**
             * @description Globally unique definition name (e.g. `tool-project-create`). This is
             *     what `parent_names` on other definitions refers to.
             */
            name: string;
            /**
             * @description Names of parent definitions. Every one must resolve within the same
             *     batch, the platform tenant, or the already-published registry — an
             *     unresolvable parent is the most common 422.
             */
            parent_names?: string[];
            title?: string;
            /** @description Interpretation tags, e.g. `interpret:tool`, `interpret:agent`. */
            tags: string[];
            /** @description Typed payload; shape depends on the interpretation tag. */
            content: {
                [key: string]: unknown;
            };
            /** @description Installation scope. Platform scope is inherited by every workspace. */
            scope?: string;
            scope_id?: string;
            /**
             * @description Bundle this item belongs to. Publishing into `engine` triggers the
             *     background engine re-sync.
             */
            bundle?: string;
        };
        RegistryPublishItem: {
            /**
             * @description Path-like registry identifier, e.g. `tools/tool-project-create`. The
             *     revision key — publishing the same stem again creates rev N+1.
             */
            stem: string;
            definition: components['schemas']['RegistryDefinition'];
        };
        /**
         * @description At least one of `items` or `bundles` must be non-empty; both empty is a
         *     400.
         */
        RegistryPublishRequest: {
            /** @description Provenance for the batch, e.g. `git:<sha>`. Recorded on every revision. */
            source_ref?: string;
            /**
             * @description Validate and diff without writing. Send this first — the response is
             *     byte-identical in shape to a real publish, so a caller can inspect
             *     exactly what it is about to do.
             */
            dry_run?: boolean;
            items?: components['schemas']['RegistryPublishItem'][];
            /** @description Bundle manifests to publish alongside the items. */
            bundles?: {
                [key: string]: unknown;
            }[];
            /**
             * @description Attest that `capabilities.tools` names resolving to no published
             *     definition are runtime-materialized (a frontend App Control tool, a
             *     grant-materialized connector). Without it, a publish containing
             *     dangling capability names is refused with 422 and a
             *     `dangling_tools` report. With it, the publish proceeds and the
             *     response records what was attested — the exception is on the
             *     record, never invisible (docs/findings/87 §6).
             */
            allow_dangling_tools?: boolean;
        };
        RegistryPublishResult: {
            stem: string;
            /**
             * @description What happened (or would happen, on a dry run). `unchanged` means the
             *     content hash matched the published revision — republishing identical
             *     content is a no-op, not an error.
             * @enum {string}
             */
            status: 'created' | 'updated' | 'unchanged';
            /** @description The revision after this publish. */
            rev?: number;
            /** @description The revision before it. Absent for a first publish. */
            prev_rev?: number;
            /** @description Content hash of the definition; what `unchanged` is decided on. */
            hash?: string;
        };
        RegistryPublishResponse: {
            dry_run: boolean;
            items: components['schemas']['RegistryPublishResult'][];
            bundles: {
                [key: string]: unknown;
            }[];
            /** @description Count of items whose status is not `unchanged`. */
            changed: number;
            /**
             * @description Present when any agent was published with unresolved
             *     `capabilities.tools` names (only possible with
             *     `allow_dangling_tools`): promptable definition name → the tool
             *     names that resolve to nothing. On the record by design.
             */
            dangling_tools?: {
                [key: string]: string[];
            };
        };
        /**
         * @description The 422 body. Not the standard error envelope — it carries a structured
         *     `validation` object instead, because "which parent did not resolve" is
         *     the actionable part and does not fit in a message string.
         */
        RegistryValidationFailure: {
            /** @enum {string} */
            error: 'validation failed';
            validation: {
                [key: string]: unknown;
            };
        };
        /** @description Where an item's `latest` currently points. */
        RegistryPointer: {
            stem?: string;
            rev?: number;
            hash?: string;
            /** @description User id, or `system:internal` for an internal-key caller. */
            published_by?: string;
            source_ref?: string;
            /** Format: date-time */
            published_at?: string;
        };
        /**
         * @description One notification row (docs/findings/87 §3). `payload` follows the
         *     kind's contract: bundle.update_available carries {bundle, scope,
         *     to_rev, summary, apply_route}; canvas.updated carries {workspace_id,
         *     page_slug, actor, summary}. Unknown kinds render as generic text.
         */
        Notification: {
            /** Format: uuid */
            id: string;
            /** @enum {string} */
            level: 'platform' | 'org' | 'workspace';
            /** @enum {string} */
            kind: 'bundle.update_available' | 'bundle.default_install_failed' | 'canvas.updated';
            payload: {
                [key: string]: unknown;
            };
            read: boolean;
            /** Format: date-time */
            created_at: string;
        };
        /**
         * @description One org bundle selection (docs/findings/87 §3): a bundle the org admin
         *     has made available to the org, optionally default for new workspaces.
         */
        OrgBundleSelection: {
            bundle: string;
            /** @enum {string} */
            source_scope: 'platform' | 'org';
            /** @description Pinned revision; absent means track latest. */
            pinned_rev?: number | null;
            is_default: boolean;
            /** Format: uuid */
            added_by?: string;
            /** Format: date-time */
            updated_at?: string;
        };
        /** @description One revision, including the definition it published. */
        RegistryRevision: {
            stem?: string;
            rev?: number;
            hash?: string;
            definition?: components['schemas']['RegistryDefinition'];
            published_by?: string;
            source_ref?: string;
            /** Format: date-time */
            published_at?: string;
        };
        CDCSubscription: {
            /** @description Pub/Sub subscription name for the per-tenant CDC topic. */
            subscription_name?: string;
            topic_name?: string;
        };
        Organization: {
            /** Format: uuid */
            id?: string;
            name?: string;
            /** Format: date-time */
            created_at?: string;
        };
        OrganizationInput: {
            name: string;
        };
        OrgMember: {
            user_id?: string;
            role?: string;
            email?: string;
        };
        OrgMemberInput: {
            email: string;
            role: string;
        };
        Tenant: {
            /** Format: uuid */
            id?: string;
            name?: string;
            /** Format: uuid */
            org_id?: string;
            /** Format: date-time */
            created_at?: string;
        };
        TenantInput: {
            name: string;
        };
        TenantMember: {
            user_id?: string;
            role?: string;
        };
        TenantMemberInput: {
            user_id: string;
            role: string;
        };
        /** @description One row the Share dialog can add or invite. `kind` tells the dialog whether the person is an existing tenant member, an org member, or a new-but-emailable address. */
        DirectoryPerson: {
            /**
             * Format: uuid
             * @description Present for existing users; absent for new email addresses.
             */
            id?: string | null;
            /** Format: email */
            email: string;
            name?: string;
            /** @enum {string} */
            kind: 'existing' | 'new' | 'already_member';
            /** @default false */
            already_member: boolean;
        };
        /** @description What the invitation-accept landing card needs before the claimee confirms — the workspace name, the role the inviter chose, and whether the invitee must set a password before they can sign in. */
        InvitationPreview: {
            /** Format: email */
            email: string;
            workspace: string;
            role: string;
            needs_password: boolean;
        };
        APIKey: {
            /** @description The tk_* API key (shown once on creation). */
            key?: string;
            /** Format: date-time */
            created_at?: string;
        };
        APIKeyStatus: {
            active?: boolean;
            /** Format: date-time */
            created_at?: string;
        };
        Usage: {
            prompt_tokens?: number;
            completion_tokens?: number;
            total_tokens?: number;
            cost_usd?: number;
        };
        BillingSubscription: {
            plan?: string;
            status?: string;
            /** Format: date-time */
            trial_ends_at?: string | null;
        };
        BillingCheckoutInput: {
            plan: string;
        };
        BillingCheckoutResult: {
            /** @description Stripe checkout URL. */
            url?: string;
        };
        FileUpload: {
            /** Format: binary */
            file: string;
        };
        FileMetadata: {
            id?: string;
            name?: string;
            mime_type?: string;
            size?: number;
            /** Format: date-time */
            created_at?: string;
        };
        SessionParticipants: {
            session_id?: string;
            participants?: {
                agent_id?: string;
                agent_name?: string;
            }[];
        };
        SessionParticipantInput: {
            agent_id: string;
        };
        /**
         * @description The `tree=true` form of `GET /v1/tree/segments`: the flat segment list
         *     alongside a nested view of the same segments.
         */
        SegmentTreeResponse: {
            segments: components['schemas']['BreadcrumbList'];
            /** @description Root segments keyed by name. */
            tree: {
                [key: string]: components['schemas']['SegmentTreeNode'];
            };
        };
        /** @description One segment in the nested view, with its hooks summarised. */
        SegmentTreeNode: {
            /** Format: uuid */
            id: string;
            name: string;
            description?: string;
            /** @description The segment defines a `before` hook. */
            has_before: boolean;
            /** @description The segment defines an `execute` hook. */
            has_execute: boolean;
            /** @description The segment defines an `after` hook. */
            has_after: boolean;
            /**
             * @description Segment names this one refuses to compose with. Declared in
             *     `meta.conflicts` and enforced by `POST /v1/compose/validate`.
             */
            conflicts?: string[];
            /**
             * @description Child segments keyed by name. Absent when the node is a leaf or when
             *     `depth` trimmed the subtree away.
             */
            children?: {
                [key: string]: components['schemas']['SegmentTreeNode'];
            };
        };
        /** @description One step of a definition's ancestry, root-first. */
        ChainLink: {
            /** Format: uuid */
            id: string;
            name: string;
        };
        TreeAddress: {
            /**
             * @description The dotted, root-first path of names.
             * @example tool-base.http-tool.weather-lookup
             */
            address: string;
            /** @description The ancestry the address was derived from, root-first. */
            chain: components['schemas']['ChainLink'][];
        };
        /**
         * @description A definition with its inherited content merged in. Compare
         *     `breadcrumb.content` with `resolved_content` to see exactly what was
         *     inherited.
         */
        ResolvedDefinition: {
            breadcrumb: components['schemas']['Breadcrumb'];
            /** @description Content after the parent chain has been merged in. */
            resolved_content: {
                [key: string]: unknown;
            };
            /**
             * @description The dotted address. Empty when it could not be derived — resolution
             *     still succeeds.
             */
            address: string;
            /** @description The parent chain, root-first. */
            chain: components['schemas']['ChainLink'][];
        };
        ResolveDiffRequest: {
            /** Format: uuid */
            child_id: string;
            /**
             * Format: uuid
             * @description Any breadcrumb to compare against — it is not required to be an
             *     actual parent of `child_id`.
             */
            parent_id: string;
        };
        ResolveDiffResult: {
            child: components['schemas']['Breadcrumb'];
            parent: components['schemas']['Breadcrumb'];
            /**
             * @description The parent's resolved content, or its own content if resolution
             *     failed.
             */
            parent_resolved: {
                [key: string]: unknown;
            };
            child_resolved: {
                [key: string]: unknown;
            };
            /** @description Top-level keys the child adds or changes, keyed by field name. */
            overrides: {
                [key: string]: components['schemas']['OverrideEntry'];
            };
        };
        OverrideEntry: {
            /** @enum {string} */
            action: 'added' | 'changed';
            /** @description The child's value. Present when `action` is `added`. */
            value?: unknown;
            /** @description The parent's value. Present when `action` is `changed`. */
            from?: unknown;
            /** @description The child's value. Present when `action` is `changed`. */
            to?: unknown;
        };
        ComposeRequest: {
            /**
             * @description Segment names in composition order, outermost first. Each must
             *     resolve to a breadcrumb tagged `interpret:segment`.
             */
            segments: string[];
            /**
             * @description Free text describing what the composition is for. Accepted and
             *     currently unused by validation.
             */
            intent?: string;
            /** @description Accepted and currently unused by validation. */
            input?: {
                [key: string]: unknown;
            };
            /** @description Accepted and currently unused by validation. */
            config?: {
                [key: string]: unknown;
            };
        };
        /**
         * @description The verdict on a composition. **A rejected composition is still a 200** —
         *     read `valid`, not the status code.
         */
        ComposeResult: {
            valid: boolean;
            pipeline?: components['schemas']['ComposePipeline'];
            /**
             * @description Why the composition was rejected — an unresolvable segment name, a
             *     breadcrumb missing the `interpret:segment` tag, or a declared
             *     conflict between two layers. Present only when `valid` is false.
             */
            error?: string;
            /** @description Reserved. Not populated. */
            result?: unknown;
            /** @description Reserved. Not populated. */
            execution_log?: unknown[];
        };
        ComposePipeline: {
            layers: components['schemas']['ComposeLayer'][];
            /** @description The dotted address of the composed pipeline. */
            address: string;
        };
        ComposeLayer: {
            name: string;
            /**
             * @description Position in the stack, 0 outermost. The deepest layer is the one
             *     that does the work.
             */
            depth: number;
            has_before: boolean;
            has_execute: boolean;
            has_after: boolean;
        };
        ComposeSuggestions: {
            /** @description The intent as submitted. */
            intent: string;
            /** @description Up to five, highest score first. May be empty. */
            suggestions: components['schemas']['ComposeSuggestion'][];
        };
        ComposeSuggestion: {
            name: string;
            description?: string;
            /**
             * @description Keyword-overlap score: 2 per intent word found in the segment name,
             *     1 per word found in its description. Not a probability, and not
             *     comparable between different intents.
             */
            score: number;
        };
        ResolvedRenderable: {
            /** Format: uuid */
            id: string;
            name: string;
            /**
             * @description The renderable's resolved content, or its own content if the parent
             *     chain could not be resolved.
             */
            content: {
                [key: string]: unknown;
            };
            tags: string[];
            /**
             * @description The wrapper stack, outermost first, with the renderable's own hooks
             *     last. Empty when the renderable has no parents and no hooks of its
             *     own. `meta.service_config` is always stripped.
             */
            layers: components['schemas']['RenderableLayer'][];
            address: string;
        };
        RenderableLayer: {
            name: string;
            depth: number;
            hooks: components['schemas']['ToolHooks'];
            /** @description Segment metadata, minus `service_config`. */
            meta?: {
                [key: string]: unknown;
            };
        };
        /**
         * @description The three composition points. Each is source the executor runs; an empty
         *     string means the layer does not participate at that point.
         */
        ToolHooks: {
            before?: string;
            execute?: string;
            after?: string;
        };
        ChangePasswordRequest: {
            /** Format: password */
            current_password: string;
            /**
             * Format: password
             * @description Must satisfy the platform password policy; a rejection names the
             *     rule that failed.
             */
            new_password: string;
        };
        SessionsRevokedResult: {
            /** @enum {string} */
            status: 'ok';
            /** @description How many other sessions ended. */
            sessions_revoked: number;
        };
        SessionRevokeResult: {
            /** @enum {string} */
            status: 'ok';
            revoked: boolean;
        };
        SessionState: {
            /** @enum {string} */
            status: 'active';
            /**
             * Format: uuid
             * @description Absent when the credential carries no session — an API key, for
             *     instance.
             */
            session_id?: string;
        };
        SessionList: {
            sessions: components['schemas']['SessionRecord'][];
            total: number;
        };
        SessionRecord: {
            /** Format: uuid */
            id: string;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            expires_at: string;
            client_ip?: string;
            /**
             * @description Truncated rather than rejected — it is unbounded client input whose
             *     only job is to help a person recognise their own device.
             */
            user_agent?: string;
            /** @description This is the session the request was made with. */
            current: boolean;
            /** Format: date-time */
            revoked_at?: string;
        };
        /** @description Counts from the cascade, so a client can confirm it actually ran. */
        DeleteAccountResult: {
            status?: string;
            /** @description Workspaces that had no members left and were deleted. */
            tenants_deleted?: number;
            /** @description Orgs that had no members left and were deleted. */
            orgs_deleted?: number;
        } & {
            [key: string]: unknown;
        };
        AggregateRequest: {
            /** @description Tags to match, ANDed. At least one, at most ten, none empty. */
            tags: string[];
            /** @enum {string} */
            fn: 'count' | 'sum' | 'avg';
            /**
             * @description Dot path within the breadcrumb content, required for `sum` and
             *     `avg`. A leading `content.` is optional — `amount` and
             *     `content.amount` are the same path. Letters, digits, underscores and
             *     dots only.
             * @example amount
             * @example content.line_total
             */
            field?: string;
            /**
             * @description Tag prefix to bucket by. `deal-stage` groups rows tagged
             *     `deal-stage:won` and `deal-stage:lost` under `won` and `lost`.
             * @example deal-stage
             */
            group_by?: string;
        };
        AggregateResult: {
            /** @description Empty when no `group_by` was requested. */
            groups: components['schemas']['AggregateGroup'][];
            /** @description The overall, ungrouped aggregate. Always present. */
            total: number;
        };
        AggregateGroup: {
            /** @description The tag value after the `group_by` prefix. */
            key: string;
            value: number;
        };
        RegistryBundleList: {
            bundles: {
                [key: string]: unknown;
            }[];
            total: number;
        };
        /**
         * @description The bundle's manifest — the definitions it installs and the version of
         *     each. Shape follows the published bundle document.
         */
        RegistryBundleManifest: {
            [key: string]: unknown;
        };
        FileText: {
            text: string;
            /** @description An estimate derived from the character count, not a count of words. */
            word_count: number;
        };
        /** @description The extraction result to store against the file. */
        FileExtractionUpdate: {
            [key: string]: unknown;
        };
        FileList: {
            files: components['schemas']['BreadcrumbList'];
            count: number;
        };
        StatusResult: {
            status: string;
        };
        KnowledgeCreateRequest: {
            /**
             * @description Your own system's identifier. It is the upsert key, so keep it
             *     stable.
             */
            external_id: string;
            /** @description The item's payload. */
            data: {
                [key: string]: unknown;
            };
            /**
             * @description Extra tags, appended after the platform's own. Do not try to set the
             *     corpus tags by hand — they are applied for you.
             */
            tags?: string[];
            /**
             * @description Who the item is for, and what role it takes to write it: `org`
             *     (org-admin), `workspace` (workspace-admin, the default), `user:<id>`
             *     or `agent:<name>` (member).
             * @default workspace
             * @example workspace
             * @example org
             * @example user:9f1c…
             * @example agent:researcher
             */
            visibility: string;
            /**
             * @description Update in place when `external_id` already exists instead of
             *     creating a second item. Set this on any write you might retry.
             * @default false
             */
            upsert: boolean;
        };
        /**
         * @description Fields to merge. Visibility is not settable — it is read from the
         *     item's existing tags. Use PUT to change scope.
         */
        KnowledgePatchRequest: {
            data?: {
                [key: string]: unknown;
            };
            tags?: string[];
        };
        KnowledgeBatchRequest: {
            /** @description Applies to every item in the batch. */
            type: string;
            items: components['schemas']['KnowledgeCreateRequest'][];
        };
        KnowledgeWriteResult: {
            /**
             * Format: uuid
             * @description The underlying breadcrumb id.
             */
            id: string;
            external_id: string;
            type: string;
            /** @enum {string} */
            action: 'created' | 'updated';
        };
        KnowledgeItem: {
            /** Format: uuid */
            id: string;
            external_id: string;
            type: string;
            /** @description Derived from the payload, not supplied by the caller. */
            title?: string;
            data: {
                [key: string]: unknown;
            };
            tags: string[];
            /** Format: date-time */
            created_at?: string;
            /** Format: date-time */
            updated_at?: string;
        };
        KnowledgeList: {
            type: string;
            items: components['schemas']['KnowledgeItem'][];
            /**
             * @description How many were returned, which is not how many exist — items scoped
             *     to another user are filtered out.
             */
            total: number;
        };
        KnowledgeBatchResult: {
            processed: number;
            created: number;
            updated: number;
            failed: number;
            items: components['schemas']['KnowledgeBatchItemResult'][];
        };
        KnowledgeBatchItemResult: {
            external_id: string;
            /**
             * Format: uuid
             * @description Absent when the item failed.
             */
            id?: string;
            /** @enum {string} */
            status: 'created' | 'updated' | 'failed';
            /** @description Present only when `status` is `failed`. */
            error?: string;
        };
        SuspendedSessions: {
            sessions?: components['schemas']['SuspendedSession'][];
            /** @description How many of the records are currently refusing turns. */
            active_count?: number;
            /** @description How long a suspension refuses turns before expiring on its own. An operator can wait this out instead of calling the reset route. */
            ttl_seconds?: number;
        };
        SuspendedSession: {
            session_id?: string;
            /** @description The live verdict. False for a cleared record and for one whose TTL has passed, even though the stored payload still says suspended. */
            suspended?: boolean;
            /** @description Why the session was suspended, e.g. `loop_detected`. */
            reason?: string;
            /** Format: date-time */
            suspended_at?: string;
            /**
             * Format: date-time
             * @description When the suspension stops refusing turns. Absent on a cleared record. Derived from the row's write time for suspensions written before suspensions carried an expiry.
             */
            expires_at?: string;
            /** @description What wrote the record — `loop_detector`, or the principal that cleared it. */
            suspended_by?: string;
            /** @description The principal that reset the suspension, if it was reset. */
            cleared_by?: string;
            /** Format: date-time */
            cleared_at?: string;
        };
        SessionSuspensionReset: {
            success?: boolean;
            session_id?: string;
            /** @description Whether the suspension was actually refusing turns when it was cleared. False means it had already expired, so the reset changed nothing an operator would have noticed. */
            was_active?: boolean;
            /** @description Always true. Clearing the flag does not reset what tripped it, so a session that is still looping is suspended again on its next event. */
            detector_still_armed?: boolean;
        };
    };
    responses: {
        /** @description Invalid request. */
        BadRequest: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                'application/json': components['schemas']['Error'];
            };
        };
        /**
         * @description Unauthorized — and on this gateway that is **two different conditions
         *     sharing one status code**. Read `error.code` before deciding what to do:
         *
         *     - `auth.workspace_not_selected` — the credential is valid; the request
         *       has no workspace scope. Re-authenticating will return the identical
         *       401 forever. The body carries `remediation` and
         *       `selectable_workspaces`; send one of those ids in `X-Tenant-ID`.
         *     - anything else, or no code — the credential is the problem. Sign in
         *       again via POST /v1/auth/login.
         */
        Unauthorized: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                'application/json': components['schemas']['Error'];
            };
        };
        /** @description Authenticated but not permitted (scope/tenant mismatch). */
        Forbidden: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                'application/json': components['schemas']['Error'];
            };
        };
        /** @description Resource not found. */
        NotFound: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                'application/json': components['schemas']['Error'];
            };
        };
        /** @description Plan limit reached (soft cap) or billing issue. */
        PaymentRequired: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                'application/json': components['schemas']['Error'];
            };
        };
        /** @description Rate limit exceeded. Retry after Retry-After header. */
        RateLimited: {
            headers: {
                'Retry-After': components['headers']['RetryAfter'];
                'X-RateLimit-Limit': components['headers']['RateLimitLimit'];
                'X-RateLimit-Remaining': components['headers']['RateLimitRemaining'];
                'X-RateLimit-Reset': components['headers']['RateLimitReset'];
                [name: string]: unknown;
            };
            content: {
                'application/json': components['schemas']['Error'];
            };
        };
        /** @description Request body too large. */
        PayloadTooLarge: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                'application/json': components['schemas']['Error'];
            };
        };
        /** @description Unexpected server error. */
        InternalError: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                'application/json': components['schemas']['Error'];
            };
        };
    };
    parameters: {
        /**
         * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
         *     every credential kind, including a session token.**
         *
         *     It used to be documented as optional for a session token, "resolved from
         *     the user's memberships". It is not: nothing infers it. Omitting it
         *     produces `401 auth.workspace_not_selected` — with a valid token, on a
         *     user who is a member of exactly one workspace. Calling
         *     POST /v1/auth/tenants/{id}/select does not change that; selection is
         *     persisted as a UI default and is not part of the session.
         *
         *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
         *     `selectable_workspaces` list, so a caller that guesses wrong is told the
         *     answer rather than left to loop.
         */
        TenantIDHeader: string;
        BreadcrumbId: string;
        SessionId: string;
        OrgId: string;
        TenantId: string;
        /**
         * @description The item's registry stem — a path-like identifier, e.g.
         *     `tools/tool-project-create`. Stable across revisions; it is what
         *     `publish` keys on.
         */
        RegistryStem: string;
        /**
         * @description The organisation whose private registry scope the route addresses
         *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
         *     platform admin; the credential must be a user session — service
         *     credentials and API keys are refused with `403 user_session_required`.
         */
        RegistryOrgID: string;
        /**
         * @description The definition's name — the portable handle. Ids are not stable between
         *     workspaces; names are, and resolution falls back to the platform tenant
         *     so a workspace that has not overridden a platform definition still finds
         *     it.
         */
        DefinitionName: string;
        /**
         * @description The knowledge corpus this item belongs to. It becomes part of the
         *     breadcrumb's tag set and is what the agent retrieval gate filters on, so
         *     two spellings of the same idea are two corpora.
         */
        KnowledgeType: string;
        /**
         * @description The identifier from the caller's own system, as supplied on write. It is
         *     the upsert key.
         */
        KnowledgeExternalId: string;
    };
    requestBodies: never;
    headers: {
        /** @description Correlation id for the request (propagated to server logs). */
        RequestId: string;
        /** @description W3C trace id (correlates with server-side distributed trace). */
        TraceId: string;
        /** @description Usage status (ok, warning, over-cap) for soft plan checks. */
        UsageStatus: 'ok' | 'warning' | 'over_cap';
        /** @description Request limit per window. */
        RateLimitLimit: number;
        /** @description Remaining requests in the current window. */
        RateLimitRemaining: number;
        /** @description Epoch seconds when the window resets. */
        RateLimitReset: number;
        /** @description Seconds to wait before retrying (429 + 503). */
        RetryAfter: number;
    };
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    getHealth: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Service healthy. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['HealthStatus'];
                };
            };
        };
    };
    getEnvConfig: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deployment configuration. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['EnvConfig'];
                };
            };
        };
    };
    register: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['RegisterRequest'];
            };
        };
        responses: {
            /** @description Accepted. Fixed body, regardless of whether an account was created. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['RegisterAccepted'];
                };
            };
            400: components['responses']['BadRequest'];
            /** @description Self-serve registration is disabled on this deployment. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            429: components['responses']['RateLimited'];
        };
    };
    login: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['LoginRequest'];
            };
        };
        responses: {
            /** @description Signed in. */
            200: {
                headers: {
                    /** @description Always `no-store` — the body carries a credential. */
                    'Cache-Control'?: string;
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['LoginResponse'];
                };
            };
            400: components['responses']['BadRequest'];
            /** @description Credential rejected. One uniform body for every cause. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /** @description The account exists and the password is correct, but the account is disabled. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /**
             * @description Either the per-address rate limit, or the account is temporarily
             *     locked after 10 consecutive failures (15 minutes, self-clearing).
             */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    getMe: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Current identity. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['MeResponse'];
                };
            };
            401: components['responses']['Unauthorized'];
        };
    };
    listMyTenants: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The caller's workspace memberships. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['TenantListResponse'];
                };
            };
            401: components['responses']['Unauthorized'];
        };
    };
    selectTenant: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Recorded. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['TenantSelectResponse'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            /** @description The caller has no access to that workspace. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    signup: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: {
            content: {
                'application/json': components['schemas']['SignupRequest'];
            };
        };
        responses: {
            /** @description Organisation + workspace created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['SignupResponse'];
                };
            };
            401: components['responses']['Unauthorized'];
            /** @description The caller already belongs to an organisation. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    logout: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Session ended. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['LogoutResponse'];
                };
            };
            401: components['responses']['Unauthorized'];
            /**
             * @description The revocation failed and the token may still be valid. Reported
             *     rather than swallowed: a sign-out the user believes happened and did
             *     not is the failure this endpoint exists to prevent.
             */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    queryBreadcrumbs: {
        parameters: {
            query?: {
                /**
                 * @description Tag filter. Repeat the parameter for multiple tags
                 *     (`?tags=a&tags=b`) — a single comma-separated value is treated as
                 *     one literal tag and will not match.
                 */
                tags?: string[];
                /**
                 * @description Exact-match on the breadcrumb name. Returns an array of 0 or 1
                 *     entries, and takes precedence over every other filter.
                 */
                name?: string;
                parent_id?: string;
                /**
                 * @description Values below 1 or absent fall back to 100; values above 1000 are
                 *     clamped to 1000.
                 */
                limit?: number;
            };
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Matching breadcrumbs, newest first. A bare array. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['BreadcrumbList'];
                };
            };
            401: components['responses']['Unauthorized'];
            429: components['responses']['RateLimited'];
        };
    };
    createBreadcrumb: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['BreadcrumbCreate'];
            };
        };
        responses: {
            /**
             * @description Existing breadcrumb updated (`upsert: true` matched an existing
             *     `name`). `action` is `updated`.
             */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['BreadcrumbWriteResult'];
                };
            };
            /** @description Breadcrumb created. `action` is `created`. */
            201: {
                headers: {
                    'X-Request-Id': components['headers']['RequestId'];
                    'X-Trace-Id': components['headers']['TraceId'];
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['BreadcrumbWriteResult'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
            429: components['responses']['RateLimited'];
        };
    };
    semanticSearchBreadcrumbs: {
        parameters: {
            query: {
                q: string;
                /** @description Repeat the parameter for multiple tags (`?tags=a&tags=b`). */
                tags?: string[];
                limit?: number;
            };
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /**
             * @description Matching breadcrumbs in descending relevance order. A bare array,
             *     with no scores.
             */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['BreadcrumbList'];
                };
            };
            /** @description `q` is missing or empty. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            401: components['responses']['Unauthorized'];
            /**
             * @description The embedding backend is unavailable or unconfigured, so no query
             *     vector could be produced.
             */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    getBreadcrumb: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                id: components['parameters']['BreadcrumbId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The breadcrumb. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Breadcrumb'];
                };
            };
            404: components['responses']['NotFound'];
        };
    };
    deleteBreadcrumb: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                id: components['parameters']['BreadcrumbId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            404: components['responses']['NotFound'];
        };
    };
    updateBreadcrumb: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                id: components['parameters']['BreadcrumbId'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['BreadcrumbUpdate'];
            };
        };
        responses: {
            /** @description Updated breadcrumb, carrying the incremented `version`. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Breadcrumb'];
                };
            };
            404: components['responses']['NotFound'];
            /**
             * @description Version conflict — the `version` sent does not match the stored one,
             *     either because it was omitted or because another writer updated the
             *     breadcrumb first. Re-read and retry.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    sendChat: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['ChatRequest'];
            };
        };
        responses: {
            /** @description Chat message accepted; pipeline started. */
            201: {
                headers: {
                    'X-Request-Id': components['headers']['RequestId'];
                    'X-RCRT-Usage-Status': components['headers']['UsageStatus'];
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['ChatResponse'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            402: components['responses']['PaymentRequired'];
            429: components['responses']['RateLimited'];
        };
    };
    streamEvents: {
        parameters: {
            query?: {
                /** @description Tag filter expression (e.g. session:abc, location:x). */
                filter?: string;
            };
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
                /** @description Resume from this event id (reconnection). */
                'Last-Event-ID'?: string;
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description SSE stream opened. */
            200: {
                headers: {
                    'Content-Type'?: 'text/event-stream';
                    'Cache-Control'?: 'no-cache';
                    Connection?: 'keep-alive';
                    [name: string]: unknown;
                };
                content: {
                    'text/event-stream': components['schemas']['SSEEventStream'];
                };
            };
            401: components['responses']['Unauthorized'];
        };
    };
    boardRender: {
        parameters: {
            query?: {
                /** @description Comma-separated focused cell ids (viewport-aware rendering). */
                viewport?: string;
            };
            header?: never;
            path: {
                /** @description The board slug. */
                slug: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The board's rendered cells. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['BoardRender'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            500: components['responses']['InternalError'];
        };
    };
    boardPresence: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The board slug. */
                slug: string;
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                'application/json': components['schemas']['BoardPresenceRequest'];
            };
        };
        responses: {
            /** @description Presence tick accepted (fire-and-forget). */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': Record<string, never>;
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
        };
    };
    streamSessionEvents: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
                'Last-Event-ID'?: string;
            };
            path: {
                session_id: components['parameters']['SessionId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Session SSE stream. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'text/event-stream': components['schemas']['SSEEventStream'];
                };
            };
            401: components['responses']['Unauthorized'];
            404: components['responses']['NotFound'];
        };
    };
    getSessionParticipants: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                session_id: components['parameters']['SessionId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Participants. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['SessionParticipants'];
                };
            };
        };
    };
    addSessionParticipant: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                session_id: components['parameters']['SessionId'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['SessionParticipantInput'];
            };
        };
        responses: {
            /** @description Participant added. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['SessionParticipants'];
                };
            };
        };
    };
    removeSessionParticipant: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                session_id: components['parameters']['SessionId'];
                agent_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Removed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            404: components['responses']['NotFound'];
        };
    };
    listSuspendedSessions: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Suspension records. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['SuspendedSessions'];
                };
            };
            403: components['responses']['Forbidden'];
        };
    };
    resetSessionSuspension: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                session_id: components['parameters']['SessionId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Suspension cleared. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['SessionSuspensionReset'];
                };
            };
            403: components['responses']['Forbidden'];
            404: components['responses']['NotFound'];
        };
    };
    createOrganization: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['OrganizationInput'];
            };
        };
        responses: {
            /** @description Created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Organization'];
                };
            };
        };
    };
    getOrganization: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The organization. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Organization'];
                };
            };
            404: components['responses']['NotFound'];
        };
    };
    updateOrganization: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['OrganizationInput'];
            };
        };
        responses: {
            /** @description Updated. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Organization'];
                };
            };
        };
    };
    deleteOrganization: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    listOrgMembers: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Members. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['OrgMember'][];
                };
            };
        };
    };
    addOrgMember: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['OrgMemberInput'];
            };
        };
        responses: {
            /** @description Added. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['OrgMember'];
                };
            };
        };
    };
    listOrgTenants: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Tenants. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Tenant'][];
                };
            };
        };
    };
    createOrgTenant: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['TenantInput'];
            };
        };
        responses: {
            /** @description Created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Tenant'];
                };
            };
        };
    };
    deleteTenant: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                tenant_id: components['parameters']['TenantId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    listTenantMembers: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                tenant_id: components['parameters']['TenantId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Members. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['TenantMember'][];
                };
            };
        };
    };
    addTenantMember: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                tenant_id: components['parameters']['TenantId'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['TenantMemberInput'];
            };
        };
        responses: {
            /** @description Added. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['TenantMember'];
                };
            };
        };
    };
    getAPIKeyStatus: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                tenant_id: components['parameters']['TenantId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Status. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['APIKeyStatus'];
                };
            };
        };
    };
    generateAPIKey: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                tenant_id: components['parameters']['TenantId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Key generated. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['APIKey'];
                };
            };
        };
    };
    revokeAPIKey: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                tenant_id: components['parameters']['TenantId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Revoked. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    getTenantUsage: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                tenant_id: components['parameters']['TenantId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Usage. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Usage'];
                };
            };
        };
    };
    getBillingSubscription: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Subscription. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['BillingSubscription'];
                };
            };
            /** @description Billing not configured (inert mode). */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    billingCheckout: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['BillingCheckoutInput'];
            };
        };
        responses: {
            /** @description Checkout URL. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['BillingCheckoutResult'];
                };
            };
        };
    };
    listFiles: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The files. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['FileList'];
                };
            };
            401: components['responses']['Unauthorized'];
        };
    };
    uploadFile: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'multipart/form-data': components['schemas']['FileUpload'];
            };
        };
        responses: {
            /** @description File uploaded. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['FileMetadata'];
                };
            };
            401: components['responses']['Unauthorized'];
            413: components['responses']['PayloadTooLarge'];
        };
    };
    getFile: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Metadata. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['FileMetadata'];
                };
            };
            404: components['responses']['NotFound'];
        };
    };
    deleteFile: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    getFileContent: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description File bytes. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/octet-stream': string;
                };
            };
        };
    };
    bulkUploadBreadcrumbs: {
        parameters: {
            query?: {
                /** @description upsert (default) or create (fail on existing name). */
                mode?: 'upsert' | 'create';
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/x-ndjson': string;
            };
        };
        responses: {
            /** @description Streaming per-row results. */
            200: {
                headers: {
                    'Content-Type'?: 'text/event-stream';
                    [name: string]: unknown;
                };
                content: {
                    'text/event-stream': components['schemas']['BulkResultStream'];
                };
            };
            401: components['responses']['Unauthorized'];
            429: components['responses']['RateLimited'];
        };
    };
    registryPublish: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['RegistryPublishRequest'];
            };
        };
        responses: {
            /**
             * @description Published, or diffed when `dry_run` was set. `changed` counts the
             *     items whose status is not `unchanged`.
             */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['RegistryPublishResponse'];
                };
            };
            /**
             * @description Malformed body, or nothing to publish (`items` and `bundles` both
             *     empty), or an item missing its `stem` or `definition.name`.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /** @description Not a platform admin. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /**
             * @description Validation failed. Nothing was written, including for the items that
             *     would have passed.
             */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['RegistryValidationFailure'];
                };
            };
        };
    };
    registryListItems: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Published items. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        items: components['schemas']['RegistryPointer'][];
                        total: number;
                    };
                };
            };
            /** @description Not a platform admin. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    registryGetItem: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The item's registry stem — a path-like identifier, e.g.
                 *     `tools/tool-project-create`. Stable across revisions; it is what
                 *     `publish` keys on.
                 */
                stem: components['parameters']['RegistryStem'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The item's latest revision. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['RegistryRevision'];
                };
            };
            /** @description Not a platform admin. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            404: components['responses']['NotFound'];
        };
    };
    registryListRevisions: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The item's registry stem — a path-like identifier, e.g.
                 *     `tools/tool-project-create`. Stable across revisions; it is what
                 *     `publish` keys on.
                 */
                stem: components['parameters']['RegistryStem'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Revision history. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        stem: string;
                        revisions: components['schemas']['RegistryRevision'][];
                        total: number;
                    };
                };
            };
            /** @description Not a platform admin. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    registryRollback: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': {
                    stem: string;
                    rev: number;
                    /** @description Override a deprecated/broken status mark on the target revision. */
                    force?: boolean;
                };
            };
        };
        responses: {
            /** @description The pointer after the rollback (includes forced/overrode_status when forced). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        pointer?: components['schemas']['RegistryPointer'];
                        forced?: boolean;
                        overrode_status?: string;
                    };
                };
            };
            /** @description `stem` missing, `rev` absent or <= 1, or no such revision. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /** @description Not a platform admin. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /** @description Target revision is deprecated/broken and `force` was not set. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    registrySetRevisionStatus: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                stem: string;
                rev: number;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': {
                    /** @enum {string} */
                    status?: '' | 'deprecated' | 'broken';
                    reason?: string;
                    superseded_by?: number;
                    source_ref_note?: string;
                };
            };
        };
        responses: {
            /** @description Updated revision envelope. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': Record<string, never>;
                };
            };
            /** @description Invalid status or unknown revision. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /** @description Not a platform admin. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    registrySyncEngine: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Synced. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        /** @enum {string} */
                        status?: 'synced';
                    };
                };
            };
            /** @description Not a platform admin. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /** @description The registry has no engine bundle yet — publish one first. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    registryOrgPublish: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['RegistryPublishRequest'];
            };
        };
        responses: {
            /** @description Published, or diffed when `dry_run` was set. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['RegistryPublishResponse'];
                };
            };
            /** @description Malformed body, nothing to publish, or an item missing its stem or definition name. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /** @description Not an org admin of this org (or not a user session). */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /** @description Validation failed, or dangling capabilities without allow_dangling_tools. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['RegistryValidationFailure'];
                };
            };
        };
    };
    registryOrgRollback: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': {
                    stem: string;
                    rev: number;
                    force?: boolean;
                };
            };
        };
        responses: {
            /** @description Rollback result — the new pointer, and what was overridden when forced. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        [key: string]: unknown;
                    };
                };
            };
            /** @description Not an org admin of this org (or not a user session). */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /** @description Target revision is marked deprecated/broken and force was not set. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    registryOrgListItems: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Published items. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        items: components['schemas']['RegistryPointer'][];
                        total: number;
                    };
                };
            };
            /** @description Not an org admin of this org (or not a user session). */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    registryOrgGetItem: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
                /**
                 * @description The item's registry stem — a path-like identifier, e.g.
                 *     `tools/tool-project-create`. Stable across revisions; it is what
                 *     `publish` keys on.
                 */
                stem: components['parameters']['RegistryStem'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The item's latest revision. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['RegistryRevision'];
                };
            };
            /** @description Not an org admin of this org (or not a user session). */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            404: components['responses']['NotFound'];
        };
    };
    registryOrgDeleteItem: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
                /**
                 * @description The item's registry stem — a path-like identifier, e.g.
                 *     `tools/tool-project-create`. Stable across revisions; it is what
                 *     `publish` keys on.
                 */
                stem: components['parameters']['RegistryStem'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Item pointer deleted. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        stem?: string;
                        /** @enum {string} */
                        status?: 'deleted';
                    };
                };
            };
            /** @description Not an org admin of this org (or not a user session). */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            404: components['responses']['NotFound'];
        };
    };
    registryOrgListRevisions: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
                /**
                 * @description The item's registry stem — a path-like identifier, e.g.
                 *     `tools/tool-project-create`. Stable across revisions; it is what
                 *     `publish` keys on.
                 */
                stem: components['parameters']['RegistryStem'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Revision history. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        stem: string;
                        revisions: components['schemas']['RegistryRevision'][];
                        total: number;
                    };
                };
            };
            /** @description Not an org admin of this org (or not a user session). */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    registryOrgListBundles: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Bundle manifests. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        bundles: components['schemas']['RegistryBundleManifest'][];
                        total: number;
                    };
                };
            };
            /** @description Not an org admin of this org (or not a user session). */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    registryOrgGetBundle: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
                /** @description Bundle name as listed by `GET /v1/registry/org/{orgID}/bundles`. */
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The bundle manifest. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['RegistryBundleManifest'];
                };
            };
            /** @description Not an org admin of this org (or not a user session). */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            404: components['responses']['NotFound'];
        };
    };
    registryOrgDeleteBundle: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description The organisation whose private registry scope the route addresses
                 *     (docs/findings/87 §2). Caller must be an org admin of this org, or a
                 *     platform admin; the credential must be a user session — service
                 *     credentials and API keys are refused with `403 user_session_required`.
                 */
                orgID: components['parameters']['RegistryOrgID'];
                /** @description Bundle name as listed by `GET /v1/registry/org/{orgID}/bundles`. */
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Bundle deleted. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        name?: string;
                        /** @enum {string} */
                        status?: 'deleted';
                    };
                };
            };
            /** @description Not an org admin of this org (or not a user session). */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            404: components['responses']['NotFound'];
        };
    };
    listMyWorkspaces: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Workspaces, deduplicated. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        workspaces: {
                            /** Format: uuid */
                            id: string;
                            name: string;
                            slug?: string;
                            /** Format: uuid */
                            org_id?: string;
                            org_name?: string;
                            is_personal: boolean;
                            /** @enum {string} */
                            visibility: 'private' | 'org' | 'public';
                            joined: boolean;
                            role?: string;
                        }[];
                        total: number;
                    };
                };
            };
            /** @description Not authenticated. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    modelPrefGet: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The agent name whose model preference this addresses. */
                agent: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The user's model preference for the agent. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['ModelPref'];
                };
            };
            401: components['responses']['Unauthorized'];
        };
    };
    modelPrefPut: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The agent name whose model preference this addresses. */
                agent: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': {
                    /** @description The catalog model id to pin for this agent. */
                    model_id: string;
                };
            };
        };
        responses: {
            /** @description The saved preference. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['ModelPref'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            /** @description Model is not available to this org (code model_not_available), or no breadcrumb write permission. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            500: components['responses']['InternalError'];
            /** @description The LLM catalog could not be loaded. */
            502: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    modelPrefDelete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The agent name whose model preference this addresses. */
                agent: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The preference was cleared (or was already absent). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['ModelPref'];
                };
            };
            401: components['responses']['Unauthorized'];
            500: components['responses']['InternalError'];
        };
    };
    updateTenantVisibility: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                tenant_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': {
                    /** @enum {string} */
                    visibility: 'private' | 'org' | 'public';
                };
            };
        };
        responses: {
            /** @description Visibility updated. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        /** Format: uuid */
                        id?: string;
                        /** Format: uuid */
                        org_id?: string;
                        name?: string;
                        slug?: string;
                        /** @enum {string} */
                        visibility?: 'private' | 'org' | 'public';
                    };
                };
            };
            /** @description Invalid tenant id, body, or visibility value. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /** @description Not authorized to manage this workspace or its org. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            404: components['responses']['NotFound'];
        };
    };
    searchTenantDirectory: {
        parameters: {
            query: {
                /** @description At least two characters; shorter queries return an empty list. */
                q: string;
            };
            header?: never;
            path: {
                tenant_id: components['parameters']['TenantId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description People matching the query. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        people: components['schemas']['DirectoryPerson'][];
                    };
                };
            };
            401: components['responses']['Unauthorized'];
            /** @description Not authorised to invite to this tenant. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            404: components['responses']['NotFound'];
        };
    };
    previewInvitation: {
        parameters: {
            query: {
                /** @description The invitation token issued by POST /v1/tenants/{tenant_id}/members. */
                token: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The invitation preview. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['InvitationPreview'];
                };
            };
            404: components['responses']['NotFound'];
        };
    };
    claimInvitation: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': {
                    /** @description The invitation token from the email / share link. */
                    token: string;
                };
            };
        };
        responses: {
            /** @description Claimed — the caller is now a member. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Tenant'];
                };
            };
            /** @description Token missing, malformed, already claimed, or expired. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            401: components['responses']['Unauthorized'];
            404: components['responses']['NotFound'];
        };
    };
    generateImage: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': {
                    prompt: string;
                    /** @description Optional size hint passed through to the image model. */
                    size?: string;
                };
            };
        };
        responses: {
            /** @description The generated image, base64-encoded. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        filename?: string;
                        mime_type?: string;
                        /** Format: byte */
                        data_base64?: string;
                        revised_prompt?: string;
                    };
                };
            };
            /** @description Prompt missing or the image slot is not an Azure deployment. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            401: components['responses']['Unauthorized'];
        };
    };
    orgBundleSelectionsList: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The organisation whose curation this addresses. Org admin of this org, or platform admin; user session required. */
                org_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The org's selections. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        selections: components['schemas']['OrgBundleSelection'][];
                        total: number;
                    };
                };
            };
            /** @description Not an org admin of this org (or not a user session). */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    orgBundleSelectionUpsert: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The organisation whose curation this addresses. Org admin of this org, or platform admin; user session required. */
                org_id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': {
                    bundle: string;
                    /**
                     * @default platform
                     * @enum {string}
                     */
                    source_scope?: 'platform' | 'org';
                    /** @description Pin to this revision; null tracks latest. */
                    pinned_rev?: number | null;
                    /** @description Install into every workspace created afterwards. */
                    is_default?: boolean;
                };
            };
        };
        responses: {
            /** @description The saved selection. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['OrgBundleSelection'];
                };
            };
            /** @description Not an org admin of this org (or not a user session). */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /** @description Bundle not found in the scope's catalogue, or the default was refused by the trigger-collision guard. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    orgBundleSelectionDelete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: string;
                bundle: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Selection deleted. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        bundle?: string;
                        /** @enum {string} */
                        status?: 'deleted';
                    };
                };
            };
            404: components['responses']['NotFound'];
        };
    };
    orgBundleSelectionUpdate: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: string;
                bundle: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': {
                    pinned_rev?: number | null;
                    is_default?: boolean;
                };
            };
        };
        responses: {
            /** @description The updated selection. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['OrgBundleSelection'];
                };
            };
            404: components['responses']['NotFound'];
            /** @description Default refused by the trigger-collision guard. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    orgAvailableModels: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The org's available chat models. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['AvailableModels'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
            /** @description The LLM catalog could not be loaded. */
            502: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    orgModelPolicyGet: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The org's model policy (or the empty no-lockdown form). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['OrgModelPolicy'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
            500: components['responses']['InternalError'];
        };
    };
    orgModelPolicyPut: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': {
                    /** @description Catalog model ids the org permits; empty = no lockdown. */
                    allowed_models: string[];
                    /** @description Optional org-default catalog model id. */
                    default_model?: string | null;
                };
            };
        };
        responses: {
            /** @description The upserted policy. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['OrgModelPolicy'];
                };
            };
            /** @description Invalid body or unknown catalog model id. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
            /** @description The LLM catalog could not be loaded. */
            502: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    orgModelPolicyDelete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Policy deleted. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        /** @enum {boolean} */
                        deleted: true;
                        /** Format: uuid */
                        org_id: string;
                    };
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
            500: components['responses']['InternalError'];
        };
    };
    orgBoardContextPolicyGet: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The org's board-context policy (or the no-lockdown default). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['OrgBoardContextPolicy'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
            500: components['responses']['InternalError'];
        };
    };
    orgBoardContextPolicyPut: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': {
                    /** @description When true, no workspace in the org feeds the board into context. */
                    locked_off: boolean;
                };
            };
        };
        responses: {
            /** @description The upserted policy. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['OrgBoardContextPolicy'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
            500: components['responses']['InternalError'];
        };
    };
    orgBoardContextPolicyDelete: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                org_id: components['parameters']['OrgId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Policy deleted. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        /** @enum {boolean} */
                        deleted: true;
                        /** Format: uuid */
                        org_id: string;
                    };
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
            500: components['responses']['InternalError'];
        };
    };
    notificationsList: {
        parameters: {
            query: {
                level: 'platform' | 'org' | 'workspace';
                /** @description Required when level=org. */
                org_id?: string;
                /** @description Required when level=workspace. */
                workspace_id?: string;
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Notifications, unread first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        notifications: components['schemas']['Notification'][];
                        unread: number;
                        total: number;
                    };
                };
            };
            /** @description Missing or invalid level/recipient parameters. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /** @description Not authorized for the named level. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    notificationsMarkAllRead: {
        parameters: {
            query: {
                level: 'platform' | 'org' | 'workspace';
                org_id?: string;
                workspace_id?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description All read. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        /** @enum {string} */
                        status?: 'read';
                    };
                };
            };
        };
    };
    notificationMarkRead: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Marked read. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        /** Format: uuid */
                        id?: string;
                        /** @enum {string} */
                        status?: 'read';
                    };
                };
            };
            /** @description Not your notification. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            404: components['responses']['NotFound'];
        };
    };
    notificationApply: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Applied; result describes what the apply did. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': {
                        /** Format: uuid */
                        id?: string;
                        /** @enum {string} */
                        status?: 'applied';
                        result?: {
                            [key: string]: unknown;
                        };
                    };
                };
            };
            /** @description Not your notification, or not a user session. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            404: components['responses']['NotFound'];
            /** @description This kind has no apply action. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /** @description The apply itself failed (e.g. bundle no longer resolvable). */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    subscribeCDC: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Subscription created. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['CDCSubscription'];
                };
            };
        };
    };
    treeRoot: {
        parameters: {
            query?: {
                /**
                 * @description Root the tree at breadcrumbs carrying this tag instead of
                 *     `interpret:segment`.
                 */
                tag?: string;
            };
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Root breadcrumbs, most recent first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['BreadcrumbList'];
                };
            };
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
        };
    };
    treeSearch: {
        parameters: {
            query: {
                /** @description Substring to match against `name` and `title`. */
                q: string;
                /** @description Restrict the scan to breadcrumbs carrying this tag. */
                tag?: string;
                /**
                 * @description Rows to scan before filtering, 1–100. Values outside that range are
                 *     ignored and the default is used.
                 */
                limit?: number;
            };
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Matching breadcrumbs. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['BreadcrumbList'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
        };
    };
    treeSegments: {
        parameters: {
            query?: {
                /**
                 * @description Set to the literal string `true` to get the nested form. Any other
                 *     value — including `1` — yields the flat array.
                 */
                tree?: 'true';
                /**
                 * @description Maximum nesting depth when `tree=true`. Ignored in flat mode, and
                 *     ignored unless it is a positive integer below 100.
                 */
                depth?: number;
            };
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /**
             * @description A bare array of segment breadcrumbs, or the `{segments, tree}`
             *     object when `tree=true`.
             */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['BreadcrumbList'] | components['schemas']['SegmentTreeResponse'];
                };
            };
            401: components['responses']['Unauthorized'];
        };
    };
    treeByName: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                /**
                 * @description The definition's name — the portable handle. Ids are not stable between
                 *     workspaces; names are, and resolution falls back to the platform tenant
                 *     so a workspace that has not overridden a platform definition still finds
                 *     it.
                 */
                name: components['parameters']['DefinitionName'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The breadcrumb. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Breadcrumb'];
                };
            };
            401: components['responses']['Unauthorized'];
            404: components['responses']['NotFound'];
        };
    };
    treeNode: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                id: components['parameters']['BreadcrumbId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The breadcrumb. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Breadcrumb'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            404: components['responses']['NotFound'];
        };
    };
    treeChildren: {
        parameters: {
            query?: {
                /** @description Return only children carrying this tag. */
                tag?: string;
            };
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                id: components['parameters']['BreadcrumbId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Child breadcrumbs. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['BreadcrumbList'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
        };
    };
    treeAncestors: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                id: components['parameters']['BreadcrumbId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The chain, child-first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['BreadcrumbList'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            404: components['responses']['NotFound'];
        };
    };
    treeAddress: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                id: components['parameters']['BreadcrumbId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The address and the chain it was derived from. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['TreeAddress'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            404: components['responses']['NotFound'];
        };
    };
    resolveById: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                id: components['parameters']['BreadcrumbId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The resolved definition. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['ResolvedDefinition'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            404: components['responses']['NotFound'];
            /**
             * @description The parent chain could not be resolved. The definition is broken,
             *     not missing.
             */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    resolveByName: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                /**
                 * @description The definition's name — the portable handle. Ids are not stable between
                 *     workspaces; names are, and resolution falls back to the platform tenant
                 *     so a workspace that has not overridden a platform definition still finds
                 *     it.
                 */
                name: components['parameters']['DefinitionName'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The resolved definition. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['ResolvedDefinition'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            404: components['responses']['NotFound'];
            /** @description The parent chain could not be resolved. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    resolveDiff: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['ResolveDiffRequest'];
            };
        };
        responses: {
            /** @description Both sides, resolved, plus the override set. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['ResolveDiffResult'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            /** @description Neither the child nor the parent id resolved. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    composeValidate: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['ComposeRequest'];
            };
        };
        responses: {
            /**
             * @description The verdict. `valid: true` carries the resolved `pipeline`;
             *     `valid: false` carries `error` naming the segment at fault.
             */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['ComposeResult'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
        };
    };
    composePreview: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['ComposeRequest'];
            };
        };
        responses: {
            /** @description The verdict — see `POST /v1/compose/validate`. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['ComposeResult'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
        };
    };
    composeSuggest: {
        parameters: {
            query: {
                /** @description What the composition should do, in words. */
                intent: string;
            };
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Up to five suggestions, best first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['ComposeSuggestions'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
        };
    };
    composeExecute: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Always. The body explains what to use instead. */
            410: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    resolveRenderable: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                /**
                 * @description The definition's name — the portable handle. Ids are not stable between
                 *     workspaces; names are, and resolution falls back to the platform tenant
                 *     so a workspace that has not overridden a platform definition still finds
                 *     it.
                 */
                name: components['parameters']['DefinitionName'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The renderable and its layer stack. */
            200: {
                headers: {
                    /** @description Always `public, max-age=60`. */
                    'Cache-Control'?: string;
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['ResolvedRenderable'];
                };
            };
            /**
             * @description The name resolved to a breadcrumb that is not tagged
             *     `interpret:renderable`.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            401: components['responses']['Unauthorized'];
            404: components['responses']['NotFound'];
        };
    };
    streamTree: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                id: components['parameters']['BreadcrumbId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The event stream. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'text/event-stream': components['schemas']['SSEEventStream'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
        };
    };
    authComplete: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The HTML page. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'text/html': string;
                };
            };
            /** @description The page asset was not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    changePassword: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['ChangePasswordRequest'];
            };
        };
        responses: {
            /** @description Password changed, other sessions ended. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['SessionsRevokedResult'];
                };
            };
            /**
             * @description Malformed body, or the new password fails the password policy. The
             *     message names the rule that failed.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /**
             * @description Not authenticated, **or** the current password is wrong. The two are
             *     deliberately not distinguished.
             */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            /**
             * @description This identity has no password set — typically one provisioned by an
             *     invitation. It needs a password *set*, which is not this endpoint.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            429: components['responses']['RateLimited'];
        };
    };
    getSessionState: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The session is live. */
            200: {
                headers: {
                    /** @description Always `no-store`. */
                    'Cache-Control'?: string;
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['SessionState'];
                };
            };
            401: components['responses']['Unauthorized'];
        };
    };
    listSessions: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The caller's sessions. */
            200: {
                headers: {
                    /** @description Always `no-store`. A session list names a person's devices. */
                    'Cache-Control'?: string;
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['SessionList'];
                };
            };
            401: components['responses']['Unauthorized'];
        };
    };
    revokeSession: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Session id, from `GET /v1/auth/sessions`. */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The session was revoked. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['SessionRevokeResult'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            /** @description No such active session, or it is not the caller's. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
        };
    };
    revokeOtherSessions: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The other sessions ended. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['SessionsRevokedResult'];
                };
            };
            401: components['responses']['Unauthorized'];
        };
    };
    deleteAccount: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The account and its orphaned resources were deleted. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['DeleteAccountResult'];
                };
            };
            401: components['responses']['Unauthorized'];
        };
    };
    aggregateBreadcrumbs: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['AggregateRequest'];
            };
        };
        responses: {
            /**
             * @description The aggregate. `groups` is empty when no `group_by` was requested;
             *     `total` always carries the overall figure.
             */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['AggregateResult'];
                };
            };
            /**
             * @description Unknown `fn`, no tags, more than 10 tags, an empty tag, or a `field`
             *     that fails the character restriction.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            401: components['responses']['Unauthorized'];
        };
    };
    listRegistryBundles: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The bundles. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['RegistryBundleList'];
                };
            };
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
        };
    };
    getRegistryBundle: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Bundle name as listed by `GET /v1/registry/bundles`. */
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The bundle manifest. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['RegistryBundleManifest'];
                };
            };
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
            404: components['responses']['NotFound'];
        };
    };
    getFileText: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                id: components['parameters']['BreadcrumbId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The extracted text. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['FileText'];
                    'text/plain': string;
                };
            };
            401: components['responses']['Unauthorized'];
            404: components['responses']['NotFound'];
        };
    };
    updateFileExtraction: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                id: components['parameters']['BreadcrumbId'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['FileExtractionUpdate'];
            };
        };
        responses: {
            /** @description The extraction was replaced. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['StatusResult'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            404: components['responses']['NotFound'];
        };
    };
    batchKnowledge: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['KnowledgeBatchRequest'];
            };
        };
        responses: {
            /**
             * @description Every item was attempted. Read `items[].status` — some may have
             *     failed.
             */
            207: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeBatchResult'];
                };
            };
            /**
             * @description Malformed body, or more than 100 items. The oversize error carries a
             *     `redirect` to the bulk-loader.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['Error'];
                };
            };
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
        };
    };
    listKnowledge: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                /**
                 * @description The knowledge corpus this item belongs to. It becomes part of the
                 *     breadcrumb's tag set and is what the agent retrieval gate filters on, so
                 *     two spellings of the same idea are two corpora.
                 */
                type: components['parameters']['KnowledgeType'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The visible items of this type. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeList'];
                };
            };
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
        };
    };
    createKnowledge: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                /**
                 * @description The knowledge corpus this item belongs to. It becomes part of the
                 *     breadcrumb's tag set and is what the agent retrieval gate filters on, so
                 *     two spellings of the same idea are two corpora.
                 */
                type: components['parameters']['KnowledgeType'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['KnowledgeCreateRequest'];
            };
        };
        responses: {
            /**
             * @description An existing item was updated — `upsert: true` matched an existing
             *     `external_id`.
             */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeWriteResult'];
                };
            };
            /** @description The item was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeWriteResult'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
        };
    };
    getKnowledge: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                /**
                 * @description The knowledge corpus this item belongs to. It becomes part of the
                 *     breadcrumb's tag set and is what the agent retrieval gate filters on, so
                 *     two spellings of the same idea are two corpora.
                 */
                type: components['parameters']['KnowledgeType'];
                /**
                 * @description The identifier from the caller's own system, as supplied on write. It is
                 *     the upsert key.
                 */
                external_id: components['parameters']['KnowledgeExternalId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The item. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeItem'];
                };
            };
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
            404: components['responses']['NotFound'];
        };
    };
    replaceKnowledge: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                /**
                 * @description The knowledge corpus this item belongs to. It becomes part of the
                 *     breadcrumb's tag set and is what the agent retrieval gate filters on, so
                 *     two spellings of the same idea are two corpora.
                 */
                type: components['parameters']['KnowledgeType'];
                /**
                 * @description The identifier from the caller's own system, as supplied on write. It is
                 *     the upsert key.
                 */
                external_id: components['parameters']['KnowledgeExternalId'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['KnowledgeCreateRequest'];
            };
        };
        responses: {
            /** @description The item was replaced. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeWriteResult'];
                };
            };
            /** @description The item did not exist and was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeWriteResult'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
        };
    };
    deleteKnowledge: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                /**
                 * @description The knowledge corpus this item belongs to. It becomes part of the
                 *     breadcrumb's tag set and is what the agent retrieval gate filters on, so
                 *     two spellings of the same idea are two corpora.
                 */
                type: components['parameters']['KnowledgeType'];
                /**
                 * @description The identifier from the caller's own system, as supplied on write. It is
                 *     the upsert key.
                 */
                external_id: components['parameters']['KnowledgeExternalId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. No body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
            404: components['responses']['NotFound'];
        };
    };
    patchKnowledge: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                /**
                 * @description The knowledge corpus this item belongs to. It becomes part of the
                 *     breadcrumb's tag set and is what the agent retrieval gate filters on, so
                 *     two spellings of the same idea are two corpora.
                 */
                type: components['parameters']['KnowledgeType'];
                /**
                 * @description The identifier from the caller's own system, as supplied on write. It is
                 *     the upsert key.
                 */
                external_id: components['parameters']['KnowledgeExternalId'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['KnowledgePatchRequest'];
            };
        };
        responses: {
            /** @description The item was updated. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeWriteResult'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
            404: components['responses']['NotFound'];
        };
    };
    customerChat: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['ChatRequest'];
            };
        };
        responses: {
            /** @description The completion, or an SSE stream when `stream` is set. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['ChatResponse'];
                    'text/event-stream': components['schemas']['SSEEventStream'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            402: components['responses']['PaymentRequired'];
            429: components['responses']['RateLimited'];
        };
    };
    customerBatchKnowledge: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['KnowledgeBatchRequest'];
            };
        };
        responses: {
            /** @description Every item was attempted. Read `items[].status`. */
            207: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeBatchResult'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
        };
    };
    customerListKnowledge: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                /**
                 * @description The knowledge corpus this item belongs to. It becomes part of the
                 *     breadcrumb's tag set and is what the agent retrieval gate filters on, so
                 *     two spellings of the same idea are two corpora.
                 */
                type: components['parameters']['KnowledgeType'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The visible items of this type. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeList'];
                };
            };
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
        };
    };
    customerCreateKnowledge: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                /**
                 * @description The knowledge corpus this item belongs to. It becomes part of the
                 *     breadcrumb's tag set and is what the agent retrieval gate filters on, so
                 *     two spellings of the same idea are two corpora.
                 */
                type: components['parameters']['KnowledgeType'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['KnowledgeCreateRequest'];
            };
        };
        responses: {
            /** @description An existing item was updated. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeWriteResult'];
                };
            };
            /** @description The item was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeWriteResult'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
        };
    };
    customerGetKnowledge: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                /**
                 * @description The knowledge corpus this item belongs to. It becomes part of the
                 *     breadcrumb's tag set and is what the agent retrieval gate filters on, so
                 *     two spellings of the same idea are two corpora.
                 */
                type: components['parameters']['KnowledgeType'];
                /**
                 * @description The identifier from the caller's own system, as supplied on write. It is
                 *     the upsert key.
                 */
                external_id: components['parameters']['KnowledgeExternalId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The item. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeItem'];
                };
            };
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
            404: components['responses']['NotFound'];
        };
    };
    customerReplaceKnowledge: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                /**
                 * @description The knowledge corpus this item belongs to. It becomes part of the
                 *     breadcrumb's tag set and is what the agent retrieval gate filters on, so
                 *     two spellings of the same idea are two corpora.
                 */
                type: components['parameters']['KnowledgeType'];
                /**
                 * @description The identifier from the caller's own system, as supplied on write. It is
                 *     the upsert key.
                 */
                external_id: components['parameters']['KnowledgeExternalId'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['KnowledgeCreateRequest'];
            };
        };
        responses: {
            /** @description The item was replaced. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeWriteResult'];
                };
            };
            /** @description The item did not exist and was created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeWriteResult'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
        };
    };
    customerDeleteKnowledge: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                /**
                 * @description The knowledge corpus this item belongs to. It becomes part of the
                 *     breadcrumb's tag set and is what the agent retrieval gate filters on, so
                 *     two spellings of the same idea are two corpora.
                 */
                type: components['parameters']['KnowledgeType'];
                /**
                 * @description The identifier from the caller's own system, as supplied on write. It is
                 *     the upsert key.
                 */
                external_id: components['parameters']['KnowledgeExternalId'];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. No body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
            404: components['responses']['NotFound'];
        };
    };
    customerPatchKnowledge: {
        parameters: {
            query?: never;
            header: {
                /**
                 * @description Tenant (workspace) scope. **Required on every tenant-scoped route, for
                 *     every credential kind, including a session token.**
                 *
                 *     It used to be documented as optional for a session token, "resolved from
                 *     the user's memberships". It is not: nothing infers it. Omitting it
                 *     produces `401 auth.workspace_not_selected` — with a valid token, on a
                 *     user who is a member of exactly one workspace. Calling
                 *     POST /v1/auth/tenants/{id}/select does not change that; selection is
                 *     persisted as a UI default and is not part of the session.
                 *
                 *     Get the id from GET /v1/auth/tenants. The 401 body also carries a
                 *     `selectable_workspaces` list, so a caller that guesses wrong is told the
                 *     answer rather than left to loop.
                 */
                'X-Tenant-ID': components['parameters']['TenantIDHeader'];
            };
            path: {
                /**
                 * @description The knowledge corpus this item belongs to. It becomes part of the
                 *     breadcrumb's tag set and is what the agent retrieval gate filters on, so
                 *     two spellings of the same idea are two corpora.
                 */
                type: components['parameters']['KnowledgeType'];
                /**
                 * @description The identifier from the caller's own system, as supplied on write. It is
                 *     the upsert key.
                 */
                external_id: components['parameters']['KnowledgeExternalId'];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                'application/json': components['schemas']['KnowledgePatchRequest'];
            };
        };
        responses: {
            /** @description The item was updated. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    'application/json': components['schemas']['KnowledgeWriteResult'];
                };
            };
            400: components['responses']['BadRequest'];
            401: components['responses']['Unauthorized'];
            403: components['responses']['Forbidden'];
            404: components['responses']['NotFound'];
        };
    };
}
//# sourceMappingURL=openapi.d.ts.map